# Solapi (알림톡/문자 발송)
SOLAPI_API_KEY=
SOLAPI_API_SECRET=
SOLAPI_SENDER=01090080851
SOLAPI_PFID=KA01PF260409120809915UFtRoM8Y2O1

# 관리자 비밀번호 — index.html의 "설정 > 관리자 비밀번호 변경"에서 바꾼 값과 반드시 동일하게 유지
# /api/send, /api/subscribe(관리자 구독)가 이 값으로 요청을 검증합니다
ADMIN_PWD=

# 웹 푸시 (기존 기능, 이미 사용 중이면 기존 값 유지)
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:admin@oheng.vercel.app

# Upstash Redis (기존 기능, 이미 사용 중이면 기존 값 유지)
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
