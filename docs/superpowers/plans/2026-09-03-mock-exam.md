# 모의고사 점수 입력·조회 기능 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 학생이 모의고사 영어 점수를 직접 입력하면, 학원 전체 동일 학년 기준 등수·추이 그래프를 학생과 선생님이 각각 볼 수 있게 한다.

**Architecture:** 무거운 `school` blob이 아닌 별도 Redis 해시 3개(`mock:rounds` / `mock:sub:<roundId>` / `mock:agg:<roundId>`)에 저장한다. 학년 통합 등수를 `HGETALL` 한 번으로 계산하고, 제출은 해시 필드 1개 쓰기라 성적 일괄저장과 쓰기 충돌이 없다. Vercel Hobby 함수 한도(12개)에 이미 도달했으므로 새 함수 파일 없이 기존 동적 라우트(`api/student/[action].js`, `api/admin/[action].js`)에 action만 추가한다.

**Tech Stack:** Node.js (Vercel Serverless), `@upstash/redis`, 프런트엔드는 빌드 없는 단일 `index.html`(순수 JS + Chart.js 4.4.1), 테스트는 `node:test` + module mock.

**Spec:** `docs/superpowers/specs/2026-09-03-mock-exam-design.md`

## Global Constraints

모든 태스크의 요구사항에 아래가 암묵적으로 포함된다.

- **`api/` 아래에 새 파일을 만들지 않는다.** Vercel Hobby 함수 12개 한도에 이미 도달했다. `api/_lib/` 아래는 번들되므로 예외.
- **학생 API 응답에 다른 학생의 이름·점수를 절대 넣지 않는다.** 집계값(평균·최고·분포·등수별 인원)만 내려보낸다.
- **등수·평균·최고·등급분포는 `closeAt` 이후이면서 응시자 5명 이상일 때만** 학생에게 내려보낸다. 선생님은 항상 본다.
- **원점수→등급 자동 환산 금지.** 등급은 학생이 고른 값만 쓴다.
- **학년 문자열은 항상 `normalizeGrade()`를 통과시킨 값으로 비교·저장한다.**
- 학생의 모든 POST는 `isSameOrigin(req)`을 확인한다.
- 회차 개설·수정·삭제는 `requireMasterAdminSessionOrApiToken`으로 마스터만.
- 점수는 `0 ~ round.maxScore` 정수, 등급은 `1~9` 정수 또는 `null`. **서버에서도 재검증한다.**
- 각 태스크 끝에 `npm test`가 통과해야 한다.
- `index.html`을 고친 태스크는 아래 문법 검사가 통과해야 한다.
  ```bash
  node -e "const fs=require('fs');const h=fs.readFileSync('index.html','utf8');const m=[...h.matchAll(/<script>([\s\S]*?)<\/script>/g)];m.forEach(x=>{new Function(x[1]);});console.log('syntax OK, blocks:',m.length)"
  ```
  기대 출력: `syntax OK, blocks: 2`
- 커밋 메시지는 한국어로 쓰고 아래 두 줄로 끝낸다.
  ```
  Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
  Claude-Session: https://claude.ai/code/session_015KDw3RTJ9Hurhdt2NhkgTa
  ```

## File Structure

| 파일 | 책임 |
|---|---|
| `api/_lib/mock.js` (신규) | 회차·제출·집계의 Redis 접근, 등수 계산, 학년 정규화, 입력값 검증. 순수 로직과 I/O를 한 파일에 두되 순수 함수(`computeAggregate`, `normalizeGrade`, `parseScore`, `parseGradeLevel`, `studentViewOf`)는 Redis 없이 단독 테스트 가능하게 export |
| `api/admin/[action].js` (수정) | 회차 CRUD·상세·수정 action 5개 추가 |
| `api/student/[action].js` (수정) | `mock` 조회·`mock-submit` action 2개 추가 |
| `tests/mock.test.mjs` (신규) | 위 세 파일의 회귀 테스트 |
| `index.html` (수정) | 학생 카드·그래프·팝업, 선생님 반 탭·회차 관리, 학교 설정 학년 드롭다운 |

---

### Task 1: `api/_lib/mock.js` — 코어 로직

**Files:**
- Create: `api/_lib/mock.js`
- Create: `tests/mock.test.mjs`

**Interfaces:**
- Consumes: `getRedis()` from `api/_lib/redis.js`, `getSchoolSummaries()` from `api/_lib/school.js`
- Produces:
  - `MIN_VISIBLE_COUNT: number` (= 5)
  - `class MockError extends Error { constructor(status, message) }`
  - `normalizeGrade(raw: string): string`
  - `parseScore(raw, maxScore): number | null` — 유효하지 않으면 `null`
  - `parseGradeLevel(raw): { ok: boolean, value: number | null }`
  - `computeAggregate(subs: Sub[]): Aggregate` — 순수 함수
  - `isRoundOpen(round, now?): boolean`
  - `studentViewOf(round, sub, agg): object`
  - `listRounds(): Promise<Round[]>` — `examDate` 내림차순
  - `getRound(id): Promise<Round|null>`
  - `saveRound(input, actorId): Promise<Round>`
  - `deleteRound(id): Promise<void>`
  - `getSubmissions(roundId): Promise<Sub[]>`
  - `getSubmission(roundId, sid): Promise<Sub|null>`
  - `countSubmissions(roundId): Promise<number>`
  - `putSubmission(roundId, sub): Promise<Aggregate>`
  - `removeSubmission(roundId, sid): Promise<Aggregate>`
  - `getAggregate(roundId): Promise<Aggregate>`
  - `recomputeAggregate(roundId): Promise<Aggregate>`
  - `countStudentsInGrade(grade): Promise<number>`

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/mock.test.mjs`를 새로 만든다. 기존 `tests/course.test.mjs`와 같은 module-mock 패턴을 쓰되, 해시 명령(`hget`/`hset`/`hgetall`/`hdel`/`hlen`)을 지원하는 가짜 Redis를 새로 만든다.

```javascript
// 모의고사 기능 회귀 테스트.
// 실제 Upstash Redis 대신 인메모리 가짜 클라이언트를 module mock으로 주입한다.
// Node 22+의 실험적 기능이라 --experimental-test-module-mocks 플래그가 필요
// (package.json의 "test" 스크립트에 이미 포함 — `npm test`로 실행).
import { test, mock, before } from 'node:test';
import assert from 'node:assert/strict';

function makeFakeRedis() {
  const store = new Map();   // 일반 키
  const hashes = new Map();  // 해시 키 -> Map(field -> value)
  const h = (k) => { if (!hashes.has(k)) hashes.set(k, new Map()); return hashes.get(k); };
  return {
    store, hashes,
    async get(key) { return store.has(key) ? store.get(key) : null; },
    async set(key, value) { store.set(key, value); return 'OK'; },
    async del(key) {
      const existed = store.has(key) || hashes.has(key);
      store.delete(key); hashes.delete(key);
      return existed ? 1 : 0;
    },
    async hget(key, field) { const m = h(key); return m.has(field) ? m.get(field) : null; },
    async hset(key, obj) {
      const m = h(key);
      Object.entries(obj).forEach(([f, v]) => m.set(f, v));
      return Object.keys(obj).length;
    },
    async hgetall(key) {
      const m = hashes.get(key);
      if (!m || m.size === 0) return null;
      return Object.fromEntries(m);
    },
    async hdel(key, field) { const m = h(key); const had = m.delete(field); return had ? 1 : 0; },
    async hlen(key) { const m = hashes.get(key); return m ? m.size : 0; },
  };
}

const fakeRedis = makeFakeRedis();
process.env.API_AUTH_TOKEN = 'test-admin-token';
// putSchoolRaw()가 학생 비밀번호를 암호화할 때 필요 (없으면 encryptPwd가 던진다)
process.env.PWD_ENC_KEY = 'test-pwd-enc-key';

before(() => {
  mock.module('../api/_lib/redis.js', {
    namedExports: { getRedis: () => fakeRedis },
  });
});

const mockLib = await import('../api/_lib/mock.js');

test('normalizeGrade: 공백과 고N 표기를 같은 키로 접는다', () => {
  assert.equal(mockLib.normalizeGrade(' 1학년 '), '1학년');
  assert.equal(mockLib.normalizeGrade('1 학년'), '1학년');
  assert.equal(mockLib.normalizeGrade('고1'), '1학년');
  assert.equal(mockLib.normalizeGrade('고 2'), '2학년');
  assert.equal(mockLib.normalizeGrade('3'), '3학년');
  assert.equal(mockLib.normalizeGrade('2학년'), '2학년');
  assert.equal(mockLib.normalizeGrade(''), '');
  assert.equal(mockLib.normalizeGrade(null), '');
});

test('computeAggregate: 동점은 같은 등수, 다음 등수는 건너뛴다', () => {
  const agg = mockLib.computeAggregate([
    { sid: 'a', schoolId: 'sc1', schoolName: 'A반', score: 88, grade: 2 },
    { sid: 'b', schoolId: 'sc1', schoolName: 'A반', score: 88, grade: 2 },
    { sid: 'c', schoolId: 'sc2', schoolName: 'B반', score: 85, grade: 3 },
  ]);
  assert.equal(agg.count, 3);
  assert.equal(agg.rankOf.a, 1);
  assert.equal(agg.rankOf.b, 1);
  assert.equal(agg.rankOf.c, 3, '동점 2명 뒤는 2등이 아니라 3등');
  assert.equal(agg.rankCounts[1], 2);
  assert.equal(agg.max, 88);
  assert.equal(agg.min, 85);
  assert.equal(agg.avg, 87);
  assert.equal(agg.gradeDist[2], 2);
  assert.equal(agg.bySchool.sc1.count, 2);
  assert.equal(agg.bySchool.sc2.avg, 85);
});

test('computeAggregate: excluded는 등수·평균·분모에서 완전히 빠진다', () => {
  const agg = mockLib.computeAggregate([
    { sid: 'a', schoolId: 'sc1', score: 100, grade: 1, excluded: true },
    { sid: 'b', schoolId: 'sc1', score: 80, grade: 3 },
    { sid: 'c', schoolId: 'sc1', score: 60, grade: 5 },
  ]);
  assert.equal(agg.count, 2, 'excluded는 분모에서도 빠져야 함');
  assert.equal(agg.avg, 70);
  assert.equal(agg.max, 80);
  assert.equal(agg.rankOf.a, undefined);
  assert.equal(agg.rankOf.b, 1);
  assert.equal(agg.gradeDist[1], undefined);
});

test('computeAggregate: 등급 미입력자는 gradeDist에서만 빠지고 등수에는 포함된다', () => {
  const agg = mockLib.computeAggregate([
    { sid: 'a', schoolId: 'sc1', score: 90, grade: null },
    { sid: 'b', schoolId: 'sc1', score: 70, grade: 4 },
  ]);
  assert.equal(agg.count, 2);
  assert.equal(agg.rankOf.a, 1);
  assert.deepEqual(agg.gradeDist, { 4: 1 });
});

test('parseScore / parseGradeLevel: 범위 밖 값은 거부한다', () => {
  assert.equal(mockLib.parseScore(88, 100), 88);
  assert.equal(mockLib.parseScore('88', 100), 88);
  assert.equal(mockLib.parseScore(0, 100), 0);
  assert.equal(mockLib.parseScore(101, 100), null);
  assert.equal(mockLib.parseScore(-1, 100), null);
  assert.equal(mockLib.parseScore(88.5, 100), null);
  assert.equal(mockLib.parseScore('', 100), null);

  assert.deepEqual(mockLib.parseGradeLevel(3), { ok: true, value: 3 });
  assert.deepEqual(mockLib.parseGradeLevel('3'), { ok: true, value: 3 });
  assert.deepEqual(mockLib.parseGradeLevel(null), { ok: true, value: null });
  assert.deepEqual(mockLib.parseGradeLevel(''), { ok: true, value: null });
  assert.equal(mockLib.parseGradeLevel(0).ok, false);
  assert.equal(mockLib.parseGradeLevel(10).ok, false);
  assert.equal(mockLib.parseGradeLevel('상').ok, false);
});

test('isRoundOpen: openAt 이상 closeAt 미만일 때만 열려 있다', () => {
  const round = { openAt: 100, closeAt: 200 };
  assert.equal(mockLib.isRoundOpen(round, 99), false);
  assert.equal(mockLib.isRoundOpen(round, 100), true);
  assert.equal(mockLib.isRoundOpen(round, 199), true);
  assert.equal(mockLib.isRoundOpen(round, 200), false, 'closeAt 시점은 이미 마감');
});

test('studentViewOf: 마감 전에는 등수·평균을 내려보내지 않는다', () => {
  const round = { id: 'r1', title: '3월 학평', examDate: '2026.03.26', grade: '1학년', maxScore: 100, closeAt: Date.now() + 100000 };
  const sub = { sid: 'a', score: 88, grade: 2 };
  const agg = { count: 40, avg: 70, max: 98, rankOf: { a: 5 }, rankCounts: { 5: 1 }, gradeDist: { 2: 10 } };
  const view = mockLib.studentViewOf(round, sub, agg);
  assert.equal(view.score, 88);
  assert.equal(view.rank, null, '마감 전에는 등수를 숨긴다');
  assert.equal(view.avg, null);
  assert.equal(view.gradeDist, null);
});

test('studentViewOf: 응시자 5명 미만이면 마감 후에도 등수·평균을 숨긴다', () => {
  const round = { id: 'r1', title: '3월 학평', examDate: '2026.03.26', grade: '1학년', maxScore: 100, closeAt: 1 };
  const sub = { sid: 'a', score: 88, grade: 2 };
  const few = { count: 4, avg: 70, max: 98, rankOf: { a: 1 }, rankCounts: { 1: 1 }, gradeDist: { 2: 1 } };
  const fewView = mockLib.studentViewOf(round, sub, few);
  assert.equal(fewView.rank, null);
  assert.equal(fewView.avg, null);
  assert.equal(fewView.count, 4, '응시자 수 자체는 보여준다');

  const many = { count: 5, avg: 70, max: 98, rankOf: { a: 1 }, rankCounts: { 1: 2 }, gradeDist: { 2: 1 } };
  const manyView = mockLib.studentViewOf(round, sub, many);
  assert.equal(manyView.rank, 1);
  assert.equal(manyView.tieCount, 2);
  assert.equal(manyView.avg, 70);
});

test('saveRound → listRounds → getRound: 회차가 저장되고 응시일 내림차순으로 나온다', async () => {
  const march = await mockLib.saveRound({
    grade: '고1', title: '2026년 3월 학력평가', examDate: '2026.03.26',
    openAt: 1, closeAt: 2, maxScore: 100,
  }, 'master');
  assert.equal(march.grade, '1학년', 'saveRound는 학년을 정규화해서 저장해야 함');
  assert.ok(march.id.startsWith('mr'));
  const second = await mockLib.saveRound({
    grade: '1학년', title: '같은 밀리초 회차', examDate: '2026.03.27',
    openAt: 1, closeAt: 2, maxScore: 100,
  }, 'master');
  assert.notEqual(second.id, march.id, '연속으로 만들어도 id가 겹치면 안 됨');

  await mockLib.saveRound({
    grade: '1학년', title: '2026년 6월 모의평가', examDate: '2026.06.04',
    openAt: 1, closeAt: 2, maxScore: 100,
  }, 'master');

  const rounds = await mockLib.listRounds();
  assert.equal(rounds.length, 3);
  assert.equal(rounds[0].examDate, '2026.06.04', '최신 회차가 먼저');

  const again = await mockLib.getRound(march.id);
  assert.equal(again.title, '2026년 3월 학력평가');
});

test('saveRound: 잘못된 입력은 MockError로 거부한다', async () => {
  await assert.rejects(
    () => mockLib.saveRound({ grade: '', title: 'x', examDate: '2026.03.26', openAt: 1, closeAt: 2 }, 'm'),
    (e) => e instanceof mockLib.MockError && e.status === 400
  );
  await assert.rejects(
    () => mockLib.saveRound({ grade: '1학년', title: '', examDate: '2026.03.26', openAt: 1, closeAt: 2 }, 'm'),
    (e) => e.status === 400
  );
  await assert.rejects(
    () => mockLib.saveRound({ grade: '1학년', title: 'x', examDate: '2026-03-26', openAt: 1, closeAt: 2 }, 'm'),
    (e) => e.status === 400
  );
  await assert.rejects(
    () => mockLib.saveRound({ grade: '1학년', title: 'x', examDate: '2026.03.26', openAt: 5, closeAt: 5 }, 'm'),
    (e) => e.status === 400
  );
});

test('putSubmission → getAggregate: 제출하면 집계가 자동으로 갱신된다', async () => {
  const round = await mockLib.saveRound({
    grade: '2학년', title: '집계 테스트', examDate: '2026.09.01',
    openAt: 1, closeAt: Date.now() + 100000, maxScore: 100,
  }, 'master');

  await mockLib.putSubmission(round.id, { sid: 's1', schoolId: 'sc1', schoolName: 'A반', name: '가', score: 90, grade: 1 });
  await mockLib.putSubmission(round.id, { sid: 's2', schoolId: 'sc1', schoolName: 'A반', name: '나', score: 70, grade: 4 });

  assert.equal(await mockLib.countSubmissions(round.id), 2);
  const agg = await mockLib.getAggregate(round.id);
  assert.equal(agg.count, 2);
  assert.equal(agg.avg, 80);
  assert.equal(agg.rankOf.s1, 1);

  const one = await mockLib.getSubmission(round.id, 's2');
  assert.equal(one.score, 70);
});

test('getAggregate: 캐시가 없으면 그 자리에서 다시 계산한다', async () => {
  const round = await mockLib.saveRound({
    grade: '3학년', title: '캐시 테스트', examDate: '2026.09.02',
    openAt: 1, closeAt: Date.now() + 100000, maxScore: 100,
  }, 'master');
  await mockLib.putSubmission(round.id, { sid: 'x1', schoolId: 'sc9', schoolName: 'C반', name: '다', score: 50, grade: 6 });

  fakeRedis.store.delete('mock:agg:' + round.id);  // 캐시 유실 상황을 흉내
  const agg = await mockLib.getAggregate(round.id);
  assert.equal(agg.count, 1, '캐시가 없어도 제출에서 다시 계산해야 함');
  assert.equal(agg.avg, 50);
});

test('removeSubmission / deleteRound: 제출과 집계가 함께 정리된다', async () => {
  const round = await mockLib.saveRound({
    grade: '1학년', title: '삭제 테스트', examDate: '2026.09.03',
    openAt: 1, closeAt: Date.now() + 100000, maxScore: 100,
  }, 'master');
  await mockLib.putSubmission(round.id, { sid: 'd1', schoolId: 'sc1', schoolName: 'A반', name: '라', score: 60, grade: 5 });
  await mockLib.putSubmission(round.id, { sid: 'd2', schoolId: 'sc1', schoolName: 'A반', name: '마', score: 80, grade: 3 });

  const afterRemove = await mockLib.removeSubmission(round.id, 'd1');
  assert.equal(afterRemove.count, 1);
  assert.equal(await mockLib.getSubmission(round.id, 'd1'), null);

  await mockLib.deleteRound(round.id);
  assert.equal(await mockLib.getRound(round.id), null);
  assert.equal(await mockLib.countSubmissions(round.id), 0);
  assert.equal(fakeRedis.store.has('mock:agg:' + round.id), false);
});
```

- [ ] **Step 2: 테스트가 실패하는지 확인**

Run: `npm test`
Expected: FAIL — `Cannot find module '../api/_lib/mock.js'`

- [ ] **Step 3: `api/_lib/mock.js` 구현**

```javascript
import { getRedis } from './redis.js';
import { getSchoolSummaries } from './school.js';

const ROUNDS_KEY = 'mock:rounds';
const subKey = (roundId) => `mock:sub:${roundId}`;
const aggKey = (roundId) => `mock:agg:${roundId}`;

// 응시자가 이보다 적으면 학생에게 등수·평균·최고점·등급분포를 숨긴다.
// 소수일 때는 평균과 내 점수만으로 남의 점수를 역산할 수 있다.
export const MIN_VISIBLE_COUNT = 5;

export class MockError extends Error {
  constructor(status, message) { super(message); this.status = status; }
}

// @upstash/redis가 JSON처럼 보이는 문자열을 알아서 객체로 되돌려주는 경우가 있어
// (school.js의 school:summary 해시에서 이미 겪은 문제) 문자열일 때만 직접 파싱한다.
function parseVal(v) {
  if (v === null || v === undefined) return null;
  if (typeof v === 'string') { try { return JSON.parse(v); } catch { return null; } }
  return v;
}

// school.grade는 자유 텍스트라 '1학년' / '고1' / ' 1 학년 '이 섞일 수 있다.
// 섞이면 등수가 두 덩어리로 쪼개져 1등이 두 명 나오는데 화면상 알아채기 어렵다.
// 회차와 반을 잇는 유일한 키이므로 비교·저장 전에 항상 이 함수를 통과시킨다.
export function normalizeGrade(raw) {
  const s = String(raw ?? '').replace(/\s+/g, '');
  if (!s) return '';
  const hs = /^고([1-3])$/.exec(s);
  if (hs) return `${hs[1]}학년`;
  const bare = /^([1-6])$/.exec(s);
  if (bare) return `${bare[1]}학년`;
  return s;
}

export function parseScore(raw, maxScore) {
  if (raw === null || raw === undefined || raw === '') return null;
  const n = Number(raw);
  if (!Number.isInteger(n) || n < 0 || n > maxScore) return null;
  return n;
}

// 등급은 "안 냄"(null)과 "잘못된 값"을 구분해야 해서 { ok, value } 형태로 돌려준다.
export function parseGradeLevel(raw) {
  if (raw === null || raw === undefined || raw === '') return { ok: true, value: null };
  const n = Number(raw);
  if (!Number.isInteger(n) || n < 1 || n > 9) return { ok: false, value: null };
  return { ok: true, value: n };
}

export function isRoundOpen(round, now = Date.now()) {
  if (!round) return false;
  return now >= round.openAt && now < round.closeAt;
}

// 순수 함수 — Redis 없이 단독으로 테스트한다.
export function computeAggregate(subs) {
  const empty = {
    count: 0, avg: null, max: null, min: null,
    rankOf: {}, rankCounts: {}, gradeDist: {}, bySchool: {}, computedAt: Date.now(),
  };
  const valid = (subs || []).filter(s => s && !s.excluded && Number.isFinite(s.score));
  if (!valid.length) return empty;

  // 표준 경쟁 등수: 동점은 같은 등수, 다음은 건너뛴다 (88·88·85 → 1·1·3).
  // 기존 성적표의 totalRank/rankCounts와 같은 규칙.
  const sorted = [...valid].sort((a, b) => b.score - a.score);
  const rankOf = {};
  const rankCounts = {};
  let rank = 0;
  sorted.forEach((s, i) => {
    if (i === 0 || s.score !== sorted[i - 1].score) rank = i + 1;
    rankOf[s.sid] = rank;
    rankCounts[rank] = (rankCounts[rank] || 0) + 1;
  });

  const scores = valid.map(s => s.score);
  const gradeDist = {};
  valid.forEach(s => {
    const g = s.grade;
    if (Number.isInteger(g) && g >= 1 && g <= 9) gradeDist[g] = (gradeDist[g] || 0) + 1;
  });

  const bySchool = {};
  valid.forEach(s => {
    const key = s.schoolId || '(미상)';
    if (!bySchool[key]) bySchool[key] = { schoolName: s.schoolName || '', count: 0, sum: 0, max: s.score };
    const b = bySchool[key];
    b.count += 1; b.sum += s.score; b.max = Math.max(b.max, s.score);
  });
  Object.values(bySchool).forEach(b => {
    b.avg = Math.round((b.sum / b.count) * 10) / 10;
    delete b.sum;
  });

  return {
    count: valid.length,
    avg: Math.round((scores.reduce((a, b) => a + b, 0) / valid.length) * 10) / 10,
    max: Math.max(...scores),
    min: Math.min(...scores),
    rankOf, rankCounts, gradeDist, bySchool,
    computedAt: Date.now(),
  };
}

// 학생에게 내려보낼 형태로 깎는다. 다른 학생의 이름·점수는 어떤 경우에도 들어가지 않는다.
export function studentViewOf(round, sub, agg, now = Date.now()) {
  const closed = now >= round.closeAt;
  const reveal = closed && (agg.count || 0) >= MIN_VISIBLE_COUNT;
  const rank = reveal ? (agg.rankOf?.[sub.sid] ?? null) : null;
  return {
    roundId: round.id,
    title: round.title,
    examDate: round.examDate,
    grade: round.grade,
    maxScore: round.maxScore,
    score: sub.score,
    myGrade: sub.grade ?? null,
    excluded: !!sub.excluded,
    rank,
    tieCount: rank != null ? (agg.rankCounts?.[rank] || 0) : null,
    count: agg.count || 0,
    avg: reveal ? agg.avg : null,
    max: reveal ? agg.max : null,
    gradeDist: reveal ? (agg.gradeDist || {}) : null,
  };
}

export async function listRounds() {
  const map = await getRedis().hgetall(ROUNDS_KEY);
  if (!map) return [];
  return Object.values(map).map(parseVal).filter(Boolean)
    .sort((a, b) => String(b.examDate || '').localeCompare(String(a.examDate || '')));
}

export async function getRound(id) {
  if (!id) return null;
  return parseVal(await getRedis().hget(ROUNDS_KEY, String(id)));
}

export async function saveRound(input, actorId) {
  const grade = normalizeGrade(input?.grade);
  if (!grade) throw new MockError(400, '학년을 선택하세요');
  const title = String(input?.title || '').trim();
  if (!title) throw new MockError(400, '회차 제목을 입력하세요');
  const examDate = String(input?.examDate || '').trim();
  if (!/^\d{4}\.\d{2}\.\d{2}$/.test(examDate)) throw new MockError(400, '응시일은 YYYY.MM.DD 형식으로 입력하세요');
  const openAt = Number(input?.openAt);
  const closeAt = Number(input?.closeAt);
  if (!Number.isFinite(openAt) || !Number.isFinite(closeAt) || closeAt <= openAt) {
    throw new MockError(400, '입력 마감일은 입력 시작일보다 뒤여야 합니다');
  }
  const maxScore = Number(input?.maxScore ?? 100);
  if (!Number.isInteger(maxScore) || maxScore < 1 || maxScore > 200) {
    throw new MockError(400, '만점은 1~200 사이 정수여야 합니다');
  }

  const existing = input?.id ? await getRound(input.id) : null;
  const round = {
    // Date.now()만 쓰면 같은 밀리초에 만든 두 회차가 같은 id를 갖는다(뒤엣것이 앞엣것을
    // 덮어씀). 뒤에 짧은 무작위 문자를 붙여 충돌을 막는다.
    id: existing?.id || ('mr' + Date.now().toString(36) + Math.random().toString(36).slice(2, 6)),
    grade, title, examDate, openAt, closeAt, maxScore,
    createdBy: existing?.createdBy || actorId || '',
    createdAt: existing?.createdAt || Date.now(),
    archived: !!input?.archived,
  };
  await getRedis().hset(ROUNDS_KEY, { [round.id]: JSON.stringify(round) });
  return round;
}

export async function deleteRound(id) {
  const redis = getRedis();
  await redis.hdel(ROUNDS_KEY, String(id));
  await redis.del(subKey(id));
  await redis.del(aggKey(id));
}

export async function getSubmissions(roundId) {
  const map = await getRedis().hgetall(subKey(roundId));
  if (!map) return [];
  return Object.values(map).map(parseVal).filter(Boolean);
}

export async function getSubmission(roundId, sid) {
  if (!sid) return null;
  return parseVal(await getRedis().hget(subKey(roundId), String(sid)));
}

export async function countSubmissions(roundId) {
  return (await getRedis().hlen(subKey(roundId))) || 0;
}

// 필드 1개 쓰기라 학생끼리도, 선생님의 성적 일괄저장과도 경쟁하지 않는다.
// 무거운 school blob의 CAS 재시도 루프를 타지 않는 게 이 구조를 고른 이유.
export async function putSubmission(roundId, sub) {
  await getRedis().hset(subKey(roundId), { [sub.sid]: JSON.stringify(sub) });
  return recomputeAggregate(roundId);
}

export async function removeSubmission(roundId, sid) {
  await getRedis().hdel(subKey(roundId), String(sid));
  return recomputeAggregate(roundId);
}

export async function recomputeAggregate(roundId) {
  const agg = computeAggregate(await getSubmissions(roundId));
  await getRedis().set(aggKey(roundId), agg);
  return agg;
}

// 캐시가 없거나 형태가 깨져 있으면 그 자리에서 다시 계산해 저장한다
// (school:summary의 self-healing과 같은 방식).
export async function getAggregate(roundId) {
  const cached = parseVal(await getRedis().get(aggKey(roundId)));
  if (cached && typeof cached.count === 'number' && cached.rankOf) return cached;
  return recomputeAggregate(roundId);
}

// 그 학년 반들의 studentCount 합. school:summary 해시만 읽으므로 학교 blob은 건드리지 않는다.
export async function countStudentsInGrade(grade) {
  const g = normalizeGrade(grade);
  if (!g) return 0;
  const summaries = await getSchoolSummaries();
  return summaries
    .filter(s => normalizeGrade(s.grade) === g)
    .reduce((n, s) => n + (s.studentCount || 0), 0);
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npm test`
Expected: PASS — `tests/mock.test.mjs`의 13개 테스트가 모두 통과하고, 기존 `tests/course.test.mjs`도 그대로 통과

- [ ] **Step 5: 커밋**

```bash
git add api/_lib/mock.js tests/mock.test.mjs
git commit -m "모의고사 코어 로직 추가 (회차·제출·등수 집계)

학년 통합 등수를 내기 위한 별도 Redis 키 공간(mock:rounds /
mock:sub:<roundId> / mock:agg:<roundId>)과 그 접근 계층.

- 표준 경쟁 등수(동점은 같은 등수, 다음은 건너뜀)로 기존 성적표 규칙과 맞춤
- 학년 문자열 정규화 — '1학년'과 '고1'이 갈라지면 1등이 두 명 나온다
- 학생에게는 마감 후 + 응시자 5명 이상일 때만 등수·평균을 노출
  (소수일 때 평균과 내 점수로 남의 점수를 역산할 수 있음)

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_015KDw3RTJ9Hurhdt2NhkgTa"
```

---

### Task 2: 관리자 API — 회차 목록·개설·삭제

**Files:**
- Modify: `api/admin/[action].js` (import 블록 + `session` 가드 뒤에 action 3개 추가)
- Modify: `tests/mock.test.mjs` (테스트 추가)

**Interfaces:**
- Consumes: Task 1의 `listRounds`, `saveRound`, `deleteRound`, `getRound`, `countSubmissions`, `normalizeGrade`, `MockError`
- Produces:
  - `GET /api/admin/mock-rounds?grade=` → `{ success, rounds: [{...round, submittedCount, totalCount}], grades: string[] }`
  - `POST /api/admin/mock-round-save` body `{ id?, grade, title, examDate, openAt, closeAt, maxScore }` → `{ success, round }`
  - `POST /api/admin/mock-round-delete` body `{ id }` → `{ success }`

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/mock.test.mjs` 맨 아래에 붙인다. 파일 상단의 `const mockLib = await import(...)` 아래에 아래 import를 추가한다.

```javascript
const auth = await import('../api/_lib/auth.js');
const school = await import('../api/_lib/school.js');
const adminHandler = (await import('../api/admin/[action].js')).default;

function makeRes() {
  const res = {
    statusCode: 200, body: undefined, ended: false,
    status(code) { res.statusCode = code; return res; },
    json(obj) { res.body = obj; return res; },
    end() { res.ended = true; return res; },
    setHeader() {},
  };
  return res;
}
// 관리자 요청은 API_AUTH_TOKEN으로 인증한다(마스터 권한까지 함께 통과).
const ADMIN_HEADERS = { authorization: 'Bearer test-admin-token', origin: 'https://x', host: 'x' };

// school.saveSchool()은 Lua 스크립트(redis.eval)로 원자적 CAS 저장을 하는데 가짜 Redis에는
// eval이 없다. 테스트에서 학교를 만들 때는 eval을 타지 않는 putSchoolRaw()를 쓴다.
async function putSchool(id, name, grade, students) {
  return school.putSchoolRaw({ id, name, grade, type: 'regular', students, records: [] });
}
```

테스트 본문:

```javascript
test('mock-round-save → mock-rounds: 회차를 만들고 제출률과 함께 목록에 나온다', async () => {
  await school.createSchool('테스트고', '고1', 'regular');

  const saveRes = makeRes();
  await adminHandler({
    method: 'POST', headers: ADMIN_HEADERS, query: { action: 'mock-round-save' },
    body: { grade: '1학년', title: 'API 회차', examDate: '2026.03.26', openAt: 1, closeAt: Date.now() + 100000, maxScore: 100 },
  }, saveRes);
  assert.equal(saveRes.statusCode, 200);
  const roundId = saveRes.body.round.id;

  const listRes = makeRes();
  await adminHandler({
    method: 'GET', headers: ADMIN_HEADERS, query: { action: 'mock-rounds', grade: '1학년' },
  }, listRes);
  assert.equal(listRes.statusCode, 200);
  const row = listRes.body.rounds.find(r => r.id === roundId);
  assert.ok(row, '방금 만든 회차가 목록에 있어야 함');
  assert.equal(row.submittedCount, 0);
  assert.ok(row.totalCount >= 0);
  assert.ok(listRes.body.grades.includes('1학년'), '현존 학년 목록을 함께 내려줘야 함');
});

test('mock-round-save: 잘못된 입력은 400으로 거부한다', async () => {
  const res = makeRes();
  await adminHandler({
    method: 'POST', headers: ADMIN_HEADERS, query: { action: 'mock-round-save' },
    body: { grade: '1학년', title: '', examDate: '2026.03.26', openAt: 1, closeAt: 2 },
  }, res);
  assert.equal(res.statusCode, 400);
  assert.match(res.body.message, /제목/);
});

test('mock-round-delete: 회차와 제출이 함께 사라진다', async () => {
  const saveRes = makeRes();
  await adminHandler({
    method: 'POST', headers: ADMIN_HEADERS, query: { action: 'mock-round-save' },
    body: { grade: '2학년', title: '지울 회차', examDate: '2026.05.01', openAt: 1, closeAt: Date.now() + 100000, maxScore: 100 },
  }, saveRes);
  const roundId = saveRes.body.round.id;
  await mockLib.putSubmission(roundId, { sid: 'z1', schoolId: 'sc1', schoolName: 'A반', name: '바', score: 70, grade: 4 });

  const delRes = makeRes();
  await adminHandler({
    method: 'POST', headers: ADMIN_HEADERS, query: { action: 'mock-round-delete' }, body: { id: roundId },
  }, delRes);
  assert.equal(delRes.statusCode, 200);
  assert.equal(await mockLib.getRound(roundId), null);
  assert.equal(await mockLib.countSubmissions(roundId), 0);
});
```

- [ ] **Step 2: 테스트가 실패하는지 확인**

Run: `npm test`
Expected: FAIL — `mock-round-save` action이 없어 404가 돌아오므로 `assert.equal(saveRes.statusCode, 200)`에서 실패

- [ ] **Step 3: 구현**

`api/admin/[action].js` 상단 import 블록에 추가한다.

```javascript
import {
  listRounds, getRound, saveRound, deleteRound,
  countSubmissions, countStudentsInGrade, normalizeGrade, MockError,
} from '../_lib/mock.js';
```

`const session = await requireAdminSessionOrApiToken(req);` 가드 **뒤**, 기존 `if (action === 'schools') {` **앞**에 삽입한다.

```javascript
  // ── 모의고사 ────────────────────────────────────────────────────────────
  // 회차는 학년 단위(여러 반 공통)라 특정 학교 blob에 담지 않고 별도 Redis 해시에 둔다.
  // 자세한 구조는 docs/superpowers/specs/2026-09-03-mock-exam-design.md 참고.

  if (action === 'mock-rounds') {
    if (req.method !== 'GET') return res.status(405).end();
    const wanted = normalizeGrade(req.query.grade || '');
    const all = await listRounds();
    const rounds = wanted ? all.filter(r => normalizeGrade(r.grade) === wanted) : all;
    // 제출률은 회차 목록의 핵심 정보라 함께 내려준다(HLEN이라 가볍다).
    const withCounts = await Promise.all(rounds.map(async (r) => ({
      ...r,
      submittedCount: await countSubmissions(r.id),
      totalCount: await countStudentsInGrade(r.grade),
    })));
    // 회차 개설 드롭다운용 — 현존 학년만 고를 수 있게 해서 '1학년'/'고1' 오타로
    // 등수가 두 덩어리로 갈라지는 사고를 원천 차단한다.
    const summaries = await getSchoolSummaries();
    const grades = [...new Set(summaries.map(s => normalizeGrade(s.grade)).filter(Boolean))].sort();
    return res.status(200).json({ success: true, rounds: withCounts, grades });
  }

  if (action === 'mock-round-save') {
    if (req.method !== 'POST') return res.status(405).end();
    if (!isSameOrigin(req)) return res.status(403).json({ success: false, message: 'Forbidden' });
    const masterCheck = await requireMasterAdminSessionOrApiToken(req);
    if (!masterCheck) return res.status(403).json({ success: false, message: '원장님 계정만 가능합니다' });
    try {
      const round = await saveRound(req.body || {}, session.actorId || '');
      return res.status(200).json({ success: true, round });
    } catch (e) {
      if (e instanceof MockError) return res.status(e.status).json({ success: false, message: e.message });
      return res.status(500).json({ success: false, message: e.message });
    }
  }

  if (action === 'mock-round-delete') {
    if (req.method !== 'POST') return res.status(405).end();
    if (!isSameOrigin(req)) return res.status(403).json({ success: false, message: 'Forbidden' });
    const masterCheck = await requireMasterAdminSessionOrApiToken(req);
    if (!masterCheck) return res.status(403).json({ success: false, message: '원장님 계정만 가능합니다' });
    const { id } = req.body || {};
    if (!id) return res.status(400).json({ success: false, message: '회차 id가 없습니다' });
    const round = await getRound(id);
    if (!round) return res.status(404).json({ success: false, message: '회차를 찾을 수 없습니다' });
    await deleteRound(id);
    return res.status(200).json({ success: true });
  }
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npm test`
Expected: PASS — 새 테스트 3개 포함 전부 통과

- [ ] **Step 5: 커밋**

```bash
git add api/admin/[action].js tests/mock.test.mjs
git commit -m "모의고사 회차 목록·개설·삭제 API 추가

Vercel Hobby 함수 12개 한도에 이미 도달해 새 함수 파일 없이
기존 admin 동적 라우트에 action만 얹었다.

개설·삭제는 마스터 계정만 통과시키고, 회차 목록에는 제출률과
현존 학년 목록(개설 드롭다운용)을 함께 내려준다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_015KDw3RTJ9Hurhdt2NhkgTa"
```

---

### Task 3: 관리자 API — 회차 상세·점수 수정

**Files:**
- Modify: `api/admin/[action].js`
- Modify: `tests/mock.test.mjs`

**Interfaces:**
- Consumes: Task 1의 `getRound`, `getSubmissions`, `getSubmission`, `putSubmission`, `getAggregate`, `parseScore`, `parseGradeLevel`, `normalizeGrade`; `getSchool`, `getSchoolIndex`, `getSchoolSummaries` from `_lib/school.js`
- Produces:
  - `GET /api/admin/mock-detail?roundId=&schoolId=` → `{ success, round, submissions, aggregate, missing: [{id,name,schoolId,schoolName}] }`
  - `POST /api/admin/mock-edit` body `{ roundId, sid, schoolId?, score?, grade?, excluded?, memo? }` → `{ success, submission, aggregate }`

`mock-edit`은 기존 제출을 고치는 것뿐 아니라 **선생님이 미제출자 대신 입력**하는 것도 처리한다. 마감 후에는 학생이 낼 방법이 없으므로, 이게 없으면 끝까지 안 낸 학생의 칸이 영영 빈다.

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/mock.test.mjs` 맨 아래에 붙인다.

```javascript
test('mock-detail: 그 반의 제출·미제출자와 학년 전체 집계를 함께 준다', async () => {
  const sc = await putSchool('sc_detail', '상세고', '3학년', [
    { id: 'sd1', name: '제출학생', pwd: '1234' },
    { id: 'sd2', name: '미제출학생', pwd: '1234' },
  ]);

  const saveRes = makeRes();
  await adminHandler({
    method: 'POST', headers: ADMIN_HEADERS, query: { action: 'mock-round-save' },
    body: { grade: '3학년', title: '상세 회차', examDate: '2026.07.01', openAt: 1, closeAt: Date.now() + 100000, maxScore: 100 },
  }, saveRes);
  const roundId = saveRes.body.round.id;
  await mockLib.putSubmission(roundId, { sid: 'sd1', schoolId: sc.id, schoolName: sc.name, name: '제출학생', score: 77, grade: 3 });

  const res = makeRes();
  await adminHandler({
    method: 'GET', headers: ADMIN_HEADERS, query: { action: 'mock-detail', roundId, schoolId: sc.id },
  }, res);
  assert.equal(res.statusCode, 200);
  assert.equal(res.body.submissions.length, 1);
  assert.equal(res.body.submissions[0].name, '제출학생');
  assert.equal(res.body.missing.length, 1);
  assert.equal(res.body.missing[0].id, 'sd2');
  assert.equal(res.body.aggregate.count, 1);
});

test('mock-edit: 선생님이 점수를 고치면 집계가 다시 계산되고 editedBy가 남는다', async () => {
  const sc = await putSchool('sc_edit', '수정고', '1학년', [{ id: 'se1', name: '수정대상', pwd: '1234' }]);

  const saveRes = makeRes();
  await adminHandler({
    method: 'POST', headers: ADMIN_HEADERS, query: { action: 'mock-round-save' },
    body: { grade: '1학년', title: '수정 회차', examDate: '2026.08.01', openAt: 1, closeAt: Date.now() + 100000, maxScore: 100 },
  }, saveRes);
  const roundId = saveRes.body.round.id;
  await mockLib.putSubmission(roundId, { sid: 'se1', schoolId: sc.id, schoolName: sc.name, name: '수정대상', score: 50, grade: 6 });

  const res = makeRes();
  await adminHandler({
    method: 'POST', headers: ADMIN_HEADERS, query: { action: 'mock-edit' },
    body: { roundId, sid: 'se1', score: 95, grade: 1 },
  }, res);
  assert.equal(res.statusCode, 200);
  assert.equal(res.body.submission.score, 95);
  assert.ok(res.body.submission.editedBy !== null, '누가 고쳤는지 남아야 함');
  assert.equal(res.body.aggregate.avg, 95);
});

test('mock-edit: 미제출자도 선생님이 대신 입력할 수 있다 (마감 후 유일한 입력 경로)', async () => {
  const sc = await putSchool('sc_proxy', '대리고', '2학년', [{ id: 'sf1', name: '대리입력', pwd: '1234' }]);

  const saveRes = makeRes();
  await adminHandler({
    method: 'POST', headers: ADMIN_HEADERS, query: { action: 'mock-round-save' },
    body: { grade: '2학년', title: '대리 회차', examDate: '2026.08.02', openAt: 1, closeAt: 2, maxScore: 100 },
  }, saveRes);
  const roundId = saveRes.body.round.id;   // 이미 마감된 회차

  const res = makeRes();
  await adminHandler({
    method: 'POST', headers: ADMIN_HEADERS, query: { action: 'mock-edit' },
    body: { roundId, sid: 'sf1', schoolId: sc.id, score: 62, grade: 5 },
  }, res);
  assert.equal(res.statusCode, 200, '마감된 회차라도 선생님은 입력할 수 있어야 함');
  assert.equal(res.body.submission.name, '대리입력', '학생 이름을 학교에서 찾아 스냅샷으로 넣어야 함');
  assert.equal(res.body.submission.score, 62);
});

test('mock-edit: excluded 처리하면 집계 분모에서 빠진다', async () => {
  const sc = await putSchool('sc_exclude', '제외고', '1학년', [
    { id: 'sg1', name: '제외대상', pwd: '1234' },
    { id: 'sg2', name: '정상', pwd: '1234' },
  ]);

  const saveRes = makeRes();
  await adminHandler({
    method: 'POST', headers: ADMIN_HEADERS, query: { action: 'mock-round-save' },
    body: { grade: '1학년', title: '제외 회차', examDate: '2026.08.03', openAt: 1, closeAt: Date.now() + 100000, maxScore: 100 },
  }, saveRes);
  const roundId = saveRes.body.round.id;
  await mockLib.putSubmission(roundId, { sid: 'sg1', schoolId: sc.id, schoolName: sc.name, name: '제외대상', score: 100, grade: 1 });
  await mockLib.putSubmission(roundId, { sid: 'sg2', schoolId: sc.id, schoolName: sc.name, name: '정상', score: 60, grade: 5 });

  const res = makeRes();
  await adminHandler({
    method: 'POST', headers: ADMIN_HEADERS, query: { action: 'mock-edit' },
    body: { roundId, sid: 'sg1', excluded: true, memo: '중복 응시' },
  }, res);
  assert.equal(res.statusCode, 200);
  assert.equal(res.body.aggregate.count, 1);
  assert.equal(res.body.aggregate.avg, 60);
});

test('mock-edit: 점수 범위 밖 값은 400으로 거부한다', async () => {
  const saveRes = makeRes();
  await adminHandler({
    method: 'POST', headers: ADMIN_HEADERS, query: { action: 'mock-round-save' },
    body: { grade: '1학년', title: '검증 회차', examDate: '2026.08.04', openAt: 1, closeAt: Date.now() + 100000, maxScore: 100 },
  }, saveRes);
  const roundId = saveRes.body.round.id;

  const res = makeRes();
  await adminHandler({
    method: 'POST', headers: ADMIN_HEADERS, query: { action: 'mock-edit' },
    body: { roundId, sid: 'sg2', schoolId: 'sc_exclude', score: 250 },
  }, res);
  assert.equal(res.statusCode, 400);
  assert.match(res.body.message, /점수/);
});
```

- [ ] **Step 2: 테스트가 실패하는지 확인**

Run: `npm test`
Expected: FAIL — `mock-detail`/`mock-edit` action이 없어 404

- [ ] **Step 3: 구현**

`api/admin/[action].js`의 mock import 블록을 아래로 교체한다.

```javascript
import {
  listRounds, getRound, saveRound, deleteRound,
  getSubmissions, getSubmission, putSubmission, getAggregate,
  countSubmissions, countStudentsInGrade,
  normalizeGrade, parseScore, parseGradeLevel, MockError,
} from '../_lib/mock.js';
```

`mock-round-delete` 블록 바로 뒤에 추가한다.

```javascript
  if (action === 'mock-detail') {
    if (req.method !== 'GET') return res.status(405).end();
    const { roundId, schoolId } = req.query;
    const round = await getRound(roundId);
    if (!round) return res.status(404).json({ success: false, message: '회차를 찾을 수 없습니다' });

    const all = await getSubmissions(roundId);
    const aggregate = await getAggregate(roundId);
    const submitted = new Set(all.map(s => s.sid));

    // 미제출자: schoolId가 있으면 그 반 blob 1개만 읽는다. 없으면(회차 관리 화면)
    // 그 학년 반들을 전부 읽는데, 이 화면에서만 일어난다.
    const roundGrade = normalizeGrade(round.grade);
    let targetSchoolIds;
    if (schoolId) {
      targetSchoolIds = [schoolId];
    } else {
      const summaries = await getSchoolSummaries();
      targetSchoolIds = summaries.filter(s => normalizeGrade(s.grade) === roundGrade).map(s => s.id);
    }
    const missing = [];
    for (const sid of targetSchoolIds) {
      const sc = await getSchool(sid);
      if (!sc) continue;
      (sc.students || []).forEach(st => {
        if (!submitted.has(st.id)) missing.push({ id: st.id, name: st.name, schoolId: sc.id, schoolName: sc.name });
      });
    }

    const submissions = schoolId ? all.filter(s => s.schoolId === schoolId) : all;
    submissions.sort((a, b) => (b.score - a.score));
    return res.status(200).json({ success: true, round, submissions, aggregate, missing });
  }

  if (action === 'mock-edit') {
    if (req.method !== 'POST') return res.status(405).end();
    if (!isSameOrigin(req)) return res.status(403).json({ success: false, message: 'Forbidden' });
    const { roundId, sid, schoolId, score, grade, excluded, memo } = req.body || {};
    if (!roundId || !sid) return res.status(400).json({ success: false, message: 'roundId와 학생 id가 필요합니다' });
    const round = await getRound(roundId);
    if (!round) return res.status(404).json({ success: false, message: '회차를 찾을 수 없습니다' });

    const prev = await getSubmission(roundId, sid);

    // 마감 후에는 학생이 낼 방법이 없으므로, 선생님의 대리 입력이 유일한 경로다.
    // 이때는 이름·반 스냅샷을 학교에서 찾아 채운다.
    let snapshot = prev;
    if (!snapshot) {
      if (!schoolId) return res.status(400).json({ success: false, message: '새로 입력하려면 schoolId가 필요합니다' });
      const sc = await getSchool(schoolId);
      const student = (sc?.students || []).find(s => s.id === sid);
      if (!student) return res.status(404).json({ success: false, message: '학생을 찾을 수 없습니다' });
      snapshot = {
        sid, schoolId: sc.id, schoolName: sc.name, name: student.name,
        score: null, grade: null, submittedAt: Date.now(), excluded: false, memo: '',
      };
    }

    const next = { ...snapshot };
    if (score !== undefined) {
      const s = parseScore(score, round.maxScore);
      if (s === null) return res.status(400).json({ success: false, message: `점수는 0~${round.maxScore} 사이 정수여야 합니다` });
      next.score = s;
    }
    if (grade !== undefined) {
      const g = parseGradeLevel(grade);
      if (!g.ok) return res.status(400).json({ success: false, message: '등급은 1~9 중에서 선택하세요' });
      next.grade = g.value;
    }
    if (excluded !== undefined) next.excluded = !!excluded;
    if (memo !== undefined) next.memo = String(memo || '').slice(0, 200);
    if (next.score === null) return res.status(400).json({ success: false, message: '점수를 입력하세요' });

    next.updatedAt = Date.now();
    next.editedBy = session.actorId || 'admin';
    const aggregate = await putSubmission(roundId, next);
    return res.status(200).json({ success: true, submission: next, aggregate });
  }
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npm test`
Expected: PASS — 새 테스트 5개 포함 전부 통과

- [ ] **Step 5: 커밋**

```bash
git add api/admin/[action].js tests/mock.test.mjs
git commit -m "모의고사 회차 상세·점수 수정 API 추가

mock-detail은 schoolId가 있으면 그 반 blob 1개만 읽어 미제출자를 계산하고,
없으면(회차 관리 화면) 그 학년 반들을 훑는다.

mock-edit은 기존 점수 수정·집계 제외뿐 아니라 선생님의 대리 입력도 받는다.
마감 후에는 학생이 낼 방법이 없어, 이게 없으면 끝까지 안 낸 학생 칸이 영영 빈다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_015KDw3RTJ9Hurhdt2NhkgTa"
```

---

### Task 4: 학생 API — 조회·제출

**Files:**
- Modify: `api/student/[action].js`
- Modify: `tests/mock.test.mjs`

**Interfaces:**
- Consumes: Task 1의 전체 export
- Produces:
  - `GET /api/student/mock` → `{ success, openRound, history }` (형태는 spec 3.1)
  - `POST /api/student/mock-submit` body `{ roundId, score, grade }` → `{ success, submission }`

**중요:** `history`는 **학년으로 거르지 않는다.** 진급은 반의 `school.grade`만 바꾸는 방식이라, 학년으로 거르면 2학년이 되는 순간 1학년 기록이 화면에서 사라진다(데이터는 남아 있는데 안 보이는 형태의 손실).

spec은 파이프라인이라 썼지만 `@upstash/redis`에서는 `Promise.all` 동시 요청이 같은 효과를 내고 코드가 단순하다.

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/mock.test.mjs` 맨 아래에 붙인다. 파일 상단 import에 아래 한 줄을 추가한다.

```javascript
const studentHandler = (await import('../api/student/[action].js')).default;
```

테스트 본문:

```javascript
test('학생 mock 조회: 열린 회차와 제출 현황이 내려온다', async () => {
  const sc = await putSchool('sc_view', '학생조회고', '1학년', [{ id: 'st1', name: '조회학생', pwd: '1234' }]);
  const { token } = await auth.createSession({ role: 'student', schoolId: sc.id, studentId: 'st1' });

  const round = await mockLib.saveRound({
    grade: '1학년', title: '학생용 회차', examDate: '2026.03.26',
    openAt: 1, closeAt: Date.now() + 100000, maxScore: 100,
  }, 'master');

  const res = makeRes();
  await studentHandler({
    method: 'GET', headers: { cookie: `oheng_session=${token}` }, query: { action: 'mock' },
  }, res);
  assert.equal(res.statusCode, 200);
  assert.equal(res.body.openRound.id, round.id);
  assert.equal(res.body.openRound.mySubmission, null);
  assert.equal(res.body.openRound.submittedCount, 0);
  // 한 파일의 테스트들이 fakeRedis를 공유하므로 같은 학년 학교가 누적된다 — 정확값 대신 하한만 본다
  assert.ok(res.body.openRound.totalCount >= 1);
  assert.deepEqual(res.body.history, []);
});

test('학생 mock-submit: 제출하면 조회에 반영되고 재제출은 덮어쓴다', async () => {
  const sc = await putSchool('sc_submit', '제출고', '2학년', [{ id: 'st2', name: '제출학생', pwd: '1234' }]);
  const { token } = await auth.createSession({ role: 'student', schoolId: sc.id, studentId: 'st2' });
  const headers = { cookie: `oheng_session=${token}`, origin: 'https://x', host: 'x' };

  const round = await mockLib.saveRound({
    grade: '2학년', title: '제출 회차', examDate: '2026.03.26',
    openAt: 1, closeAt: Date.now() + 100000, maxScore: 100,
  }, 'master');

  const res1 = makeRes();
  await studentHandler({ method: 'POST', headers, query: { action: 'mock-submit' }, body: { roundId: round.id, score: 88, grade: 2 } }, res1);
  assert.equal(res1.statusCode, 200);

  const res2 = makeRes();
  await studentHandler({ method: 'POST', headers, query: { action: 'mock-submit' }, body: { roundId: round.id, score: 91, grade: 1 } }, res2);
  assert.equal(res2.statusCode, 200);

  assert.equal(await mockLib.countSubmissions(round.id), 1, '재제출은 새 행이 아니라 덮어쓰기');
  const stored = await mockLib.getSubmission(round.id, 'st2');
  assert.equal(stored.score, 91);
  assert.equal(stored.name, '제출학생', '이름을 스냅샷으로 남겨야 함');
  assert.equal(stored.schoolName, '제출고');
});

test('학생 mock-submit: 마감된 회차와 다른 학년 회차는 거부한다', async () => {
  const sc = await putSchool('sc_reject', '거부고', '3학년', [{ id: 'st3', name: '거부학생', pwd: '1234' }]);
  const { token } = await auth.createSession({ role: 'student', schoolId: sc.id, studentId: 'st3' });
  const headers = { cookie: `oheng_session=${token}`, origin: 'https://x', host: 'x' };

  const closed = await mockLib.saveRound({ grade: '3학년', title: '마감됨', examDate: '2026.01.01', openAt: 1, closeAt: 2, maxScore: 100 }, 'm');
  const resClosed = makeRes();
  await studentHandler({ method: 'POST', headers, query: { action: 'mock-submit' }, body: { roundId: closed.id, score: 50 } }, resClosed);
  assert.equal(resClosed.statusCode, 403);
  assert.match(resClosed.body.message, /마감/);

  const otherGrade = await mockLib.saveRound({ grade: '1학년', title: '남의 학년', examDate: '2026.04.01', openAt: 1, closeAt: Date.now() + 100000, maxScore: 100 }, 'm');
  const resGrade = makeRes();
  await studentHandler({ method: 'POST', headers, query: { action: 'mock-submit' }, body: { roundId: otherGrade.id, score: 50 } }, resGrade);
  assert.equal(resGrade.statusCode, 403);
});

test('학생 mock-submit: 점수·등급 검증을 서버에서 다시 한다', async () => {
  const sc = await putSchool('sc_valid', '검증고', '1학년', [{ id: 'st4', name: '검증학생', pwd: '1234' }]);
  const { token } = await auth.createSession({ role: 'student', schoolId: sc.id, studentId: 'st4' });
  const headers = { cookie: `oheng_session=${token}`, origin: 'https://x', host: 'x' };
  const round = await mockLib.saveRound({ grade: '1학년', title: '검증', examDate: '2026.04.02', openAt: 1, closeAt: Date.now() + 100000, maxScore: 100 }, 'm');

  const resScore = makeRes();
  await studentHandler({ method: 'POST', headers, query: { action: 'mock-submit' }, body: { roundId: round.id, score: 120 } }, resScore);
  assert.equal(resScore.statusCode, 400);

  const resGrade = makeRes();
  await studentHandler({ method: 'POST', headers, query: { action: 'mock-submit' }, body: { roundId: round.id, score: 80, grade: 12 } }, resGrade);
  assert.equal(resGrade.statusCode, 400);
});

test('학생 mock 조회: 학년이 올라가도 지난 학년 회차가 history에 남는다', async () => {
  const sc = await putSchool('sc_promote', '진급고', '1학년', [{ id: 'st5', name: '진급학생', pwd: '1234' }]);
  const { token } = await auth.createSession({ role: 'student', schoolId: sc.id, studentId: 'st5' });

  // 1학년 때 본 마감된 회차 — 응시자 5명을 채워 등수가 공개되는 상태로 만든다
  const g1 = await mockLib.saveRound({ grade: '1학년', title: '1학년 3월', examDate: '2026.03.26', openAt: 1, closeAt: 2, maxScore: 100 }, 'm');
  await mockLib.putSubmission(g1.id, { sid: 'st5', schoolId: sc.id, schoolName: sc.name, name: '진급학생', score: 88, grade: 2 });
  for (let i = 0; i < 4; i++) {
    await mockLib.putSubmission(g1.id, { sid: 'other' + i, schoolId: sc.id, schoolName: sc.name, name: '기타', score: 60 + i, grade: 5 });
  }

  // 반이 통째로 2학년이 됨 — 학교의 학년만 바꾼다 (반도 학생도 그대로)
  await putSchool('sc_promote', '진급고', '2학년', [{ id: 'st5', name: '진급학생', pwd: '1234' }]);

  const res = makeRes();
  await studentHandler({ method: 'GET', headers: { cookie: `oheng_session=${token}` }, query: { action: 'mock' } }, res);
  assert.equal(res.statusCode, 200);
  const past = res.body.history.find(h => h.roundId === g1.id);
  assert.ok(past, '2학년이 되어도 1학년 회차가 history에 남아야 함');
  assert.equal(past.score, 88);
  assert.equal(past.rank, 1);
  assert.equal(past.count, 5);
  assert.equal(past.avg, 66.8);  // (88+60+61+62+63)/5
  // 다른 학생 정보가 새어나가지 않는지
  assert.equal(JSON.stringify(res.body).includes('기타'), false, '다른 학생 이름이 응답에 있으면 안 됨');
});
```

- [ ] **Step 2: 테스트가 실패하는지 확인**

Run: `npm test`
Expected: FAIL — `mock` action이 없어 404

- [ ] **Step 3: 구현**

`api/student/[action].js` 상단 import 아래에 추가한다.

```javascript
import {
  listRounds, getRound, getSubmission, putSubmission, getAggregate,
  countSubmissions, countStudentsInGrade,
  normalizeGrade, parseScore, parseGradeLevel, isRoundOpen, studentViewOf,
} from '../_lib/mock.js';
```

`if (action === 'suggestion-read') { ... }` 블록 뒤, 마지막 `return res.status(404)` 앞에 삽입한다.

```javascript
  if (action === 'mock') {
    if (req.method !== 'GET') return res.status(405).end();
    const sc = await getSchool(session.schoolId);
    if (!sc) return res.status(404).json({ success: false, message: '학교를 찾을 수 없습니다' });
    const myGrade = normalizeGrade(sc.grade);
    const rounds = await listRounds();
    const now = Date.now();

    // 입력칸을 띄울 회차 — 지금 내 학년 기준
    const open = rounds.find(r => !r.archived && normalizeGrade(r.grade) === myGrade && isRoundOpen(r, now));
    let openRound = null;
    if (open) {
      const [mySub, submittedCount, totalCount] = await Promise.all([
        getSubmission(open.id, session.studentId),
        countSubmissions(open.id),
        countStudentsInGrade(myGrade),
      ]);
      openRound = {
        id: open.id, title: open.title, examDate: open.examDate,
        closeAt: open.closeAt, maxScore: open.maxScore,
        mySubmission: mySub ? { score: mySub.score, grade: mySub.grade ?? null, updatedAt: mySub.updatedAt } : null,
        submittedCount, totalCount,
      };
    }

    // 지난 기록 — 학년으로 거르지 않는다. 진급은 반의 학년만 바꾸는 방식이라
    // 학년으로 거르면 2학년이 되는 순간 1학년 기록이 화면에서 사라진다
    // (데이터는 남아 있는데 안 보이는, 알아채기 어려운 형태의 손실).
    const closedRounds = rounds.filter(r => now >= r.closeAt);
    const pairs = await Promise.all(closedRounds.map(async (r) => {
      const sub = await getSubmission(r.id, session.studentId);
      if (!sub) return null;
      const agg = await getAggregate(r.id);
      return studentViewOf(r, sub, agg, now);
    }));
    const history = pairs.filter(Boolean)
      .sort((a, b) => String(a.examDate || '').localeCompare(String(b.examDate || '')));

    return res.status(200).json({ success: true, openRound, history });
  }

  if (action === 'mock-submit') {
    if (req.method !== 'POST') return res.status(405).end();
    if (!isSameOrigin(req)) return res.status(403).json({ success: false, message: 'Forbidden' });
    const { roundId, score, grade } = req.body || {};
    const round = await getRound(roundId);
    if (!round) return res.status(404).json({ success: false, message: '회차를 찾을 수 없습니다' });
    if (!isRoundOpen(round)) return res.status(403).json({ success: false, message: '입력이 마감되었습니다' });

    const sc = await getSchool(session.schoolId);
    if (!sc) return res.status(404).json({ success: false, message: '학교를 찾을 수 없습니다' });
    if (normalizeGrade(sc.grade) !== normalizeGrade(round.grade)) {
      return res.status(403).json({ success: false, message: '학년이 맞지 않는 회차입니다' });
    }
    const student = (sc.students || []).find(s => s.id === session.studentId);
    if (!student) return res.status(404).json({ success: false, message: '학생 정보를 찾을 수 없습니다' });

    const s = parseScore(score, round.maxScore);
    if (s === null) return res.status(400).json({ success: false, message: `점수는 0~${round.maxScore} 사이 정수로 입력하세요` });
    const g = parseGradeLevel(grade);
    if (!g.ok) return res.status(400).json({ success: false, message: '등급은 1~9 중에서 선택하세요' });

    const prev = await getSubmission(roundId, session.studentId);
    const now = Date.now();
    const sub = {
      sid: session.studentId,
      schoolId: sc.id, schoolName: sc.name, name: student.name,
      score: s, grade: g.value,
      submittedAt: prev?.submittedAt || now,
      updatedAt: now,
      editedBy: null,
      // 선생님이 집계에서 제외해둔 학생이 다시 제출한다고 제외가 풀리면 안 된다
      excluded: prev?.excluded || false,
      memo: prev?.memo || '',
    };
    await putSubmission(roundId, sub);
    return res.status(200).json({ success: true, submission: { score: sub.score, grade: sub.grade, updatedAt: sub.updatedAt } });
  }
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npm test`
Expected: PASS — 새 테스트 5개 포함 전부 통과

- [ ] **Step 5: 커밋**

```bash
git add api/student/[action].js tests/mock.test.mjs
git commit -m "학생 모의고사 조회·제출 API 추가

history는 학년으로 거르지 않는다. 진급이 반의 학년만 바꾸는 방식이라
학년으로 거르면 2학년이 되는 순간 1학년 기록이 화면에서 사라진다
(데이터는 남는데 안 보이는, 알아채기 어려운 형태의 손실).

제출은 해시 필드 1개 쓰기라 성적 일괄저장과 경쟁하지 않는다.
선생님이 집계에서 제외해둔 학생이 재제출해도 제외가 풀리지 않게 했다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_015KDw3RTJ9Hurhdt2NhkgTa"
```

---

### Task 5: 학생 화면 — 성적표 상단 모의고사 카드

**Files:**
- Modify: `index.html` — `rStudent()` (2260행 근처), `bStudent()` (2670행 근처), `loadServerStudentData()` (1530행 근처)

**Interfaces:**
- Consumes: `GET /api/student/mock`, `POST /api/student/mock-submit`
- Produces (전역 함수·상태, 이후 태스크가 쓴다):
  - `ST.mock: { openRound, history } | null`
  - `async function loadStudentMock(): Promise<void>` — `ST.mock`을 채운다
  - `function rMockCard(): string` — 카드 HTML (모의고사가 없으면 빈 문자열)
  - `function bMockCard(): void` — 카드 이벤트 바인딩
  - `async function submitMockScore(roundId, score, grade, msgEl): Promise<boolean>`

- [ ] **Step 1: 데이터 로딩 추가**

`loadServerStudentData()` 안에서 `applyServerStudentData(data)` 호출 **뒤**에 `await loadStudentMock();`를 넣는다. 그리고 그 함수 바로 위에 아래를 추가한다.

```javascript
// 모의고사 데이터는 성적표(/api/student/me)와 별도 엔드포인트라 따로 불러온다.
// 실패해도 성적표 자체는 그대로 보여야 하므로 조용히 null로 둔다.
async function loadStudentMock(){
  try{
    const res=await fetch('/api/student/mock',{credentials:'include'});
    if(!res.ok){ST.mock=null;return;}
    const data=await res.json();
    ST.mock=data.success?{openRound:data.openRound,history:data.history||[]}:null;
  }catch(e){ST.mock=null;}
}
```

- [ ] **Step 2: 카드 렌더 함수 추가**

`function rReport(recs){` **바로 위**에 추가한다.

```javascript
// 모의고사 카드 — 성적표 탭 맨 위. 세 가지 상태를 가진다.
//  1) 열린 회차 미제출  → 입력 폼
//  2) 열린 회차 제출완료 → 제출값 + 수정 버튼 (등수는 마감 후에만 공개하므로 여기선 안 보여줌)
//  3) 열린 회차 없음     → 가장 최근 마감 회차의 결과 카드
// 등급은 자유 입력이 아니라 1~9 드롭다운만 받는다. 표기가 흔들리면 나중에 필터링이 안 된다.
function mockGradeOptions(sel){
  let out=`<option value="" ${sel==null?'selected':''}>모름</option>`;
  for(let g=1;g<=9;g++) out+=`<option value="${g}" ${sel===g?'selected':''}>${g}등급</option>`;
  return out;
}
function mockRatioText(r){
  if(!r||r.totalCount==null)return'';
  return `우리 학년 ${r.totalCount}명 중 <b style="color:#1E40AF">${r.submittedCount}명</b> 입력 완료`;
}
function mockDdayBadge(closeAt){
  const left=closeAt-Date.now();
  if(left<=0)return'';
  const urgent=left<24*60*60*1000;
  const d=new Date(closeAt);
  const label=`마감 ${d.getMonth()+1}/${d.getDate()}`;
  return `<span style="font-size:11px;font-weight:800;padding:3px 8px;border-radius:6px;${urgent?'background:#FFEBEE;color:#C62828':'background:#EEF1F5;color:#57616F'}">${label}</span>`;
}
function rMockInputForm(r){
  const cur=r.mySubmission;
  return `
  <div style="display:flex;gap:8px;align-items:flex-end;margin-top:12px">
    <div style="flex:1">
      <label style="display:block;font-size:11px;font-weight:700;color:#57616F;margin-bottom:5px">영어 원점수</label>
      <input id="mock-score" type="number" inputmode="numeric" min="0" max="${r.maxScore}" value="${cur?cur.score:''}" placeholder="0~${r.maxScore}"
        style="width:100%;padding:10px 12px;border:1.5px solid #D9E0EA;border-radius:9px;font-size:15px;font-weight:700;outline:none;font-family:inherit;color:#10192B">
    </div>
    <div style="flex:1">
      <label style="display:block;font-size:11px;font-weight:700;color:#57616F;margin-bottom:5px">등급</label>
      <select id="mock-grade" style="width:100%;padding:10px 12px;border:1.5px solid #D9E0EA;border-radius:9px;font-size:14px;font-weight:700;outline:none;font-family:inherit;background:#fff;color:#10192B">
        ${mockGradeOptions(cur?cur.grade:null)}
      </select>
    </div>
  </div>
  <button id="mock-submit" style="width:100%;margin-top:12px;padding:12px;background:#1E40AF;color:#fff;border:none;border-radius:10px;font-size:14px;font-weight:800;cursor:pointer;font-family:inherit">${cur?'수정하기':'제출하기'}</button>
  <div id="mock-msg" style="font-size:12px;margin-top:8px;min-height:16px"></div>`;
}
function rMockResultCard(h){
  const pf="font-family:'Public Sans',Pretendard,-apple-system,sans-serif";
  const rankLine = h.rank!=null
    ? `학년 ${h.rank}등 / ${h.count}명${h.tieCount>1?` <span style="color:#8C93A0">(동점 ${h.tieCount}명)</span>`:''}`
    : (h.count<5 ? `응시자 ${h.count}명 — 응시자가 적어 등수는 표시하지 않습니다` : '마감 후 등수가 공개됩니다');
  const dist=h.gradeDist||null;
  const distMax=dist?Math.max(1,...Object.values(dist)):1;
  const distBars=dist?`
    <div style="display:flex;align-items:flex-end;gap:5px;height:70px;margin-top:14px">
      ${[1,2,3,4,5,6,7,8,9].map(g=>{
        const n=dist[g]||0;const mine=h.myGrade===g;
        return `<div style="flex:1;display:flex;flex-direction:column;align-items:center;gap:4px">
          <div style="font-size:9.5px;font-weight:700;color:${mine?'#1E40AF':'#8C93A0'}">${n||''}</div>
          <div style="width:100%;height:${Math.round(n/distMax*44)}px;min-height:2px;border-radius:3px 3px 0 0;background:${mine?'#1E40AF':'#D9E0EA'}"></div>
          <div style="font-size:9.5px;font-weight:${mine?'800':'600'};color:${mine?'#1E40AF':'#8C93A0'}">${g}</div>
        </div>`;
      }).join('')}
    </div>
    <div style="font-size:10.5px;color:#8C93A0;text-align:center;margin-top:4px">학년 등급 분포 (내 등급 강조)</div>`:'';
  return `
  <div style="display:flex;justify-content:space-between;align-items:baseline;gap:8px">
    <div style="font-size:14px;font-weight:800;color:#10192B">${esc(h.title)}</div>
    <div style="font-size:11px;font-weight:700;color:#8C93A0">${esc(h.grade)} · ${esc(h.examDate)}</div>
  </div>
  <div style="font-size:34px;font-weight:800;line-height:1.1;color:#1E40AF;margin-top:8px;font-variant-numeric:tabular-nums;${pf}">
    ${h.score}<span style="font-size:14px;font-weight:700;color:#57616F;margin-left:5px">점${h.myGrade?` · ${h.myGrade}등급`:''}</span>
  </div>
  <div style="font-size:12.5px;font-weight:700;color:#57616F;margin-top:6px">${rankLine}</div>
  ${h.avg!=null?`<div style="font-size:12px;color:#57616F;margin-top:3px">학년 평균 ${h.avg}점 · 최고 ${h.max}점</div>`:''}
  ${distBars}`;
}
function rMockCard(){
  const m=ST.mock;
  if(!m)return'';
  const open=m.openRound;
  const last=(m.history&&m.history.length)?m.history[m.history.length-1]:null;
  if(!open&&!last)return'';

  let inner;
  if(open){
    inner=`
    <div style="display:flex;justify-content:space-between;align-items:center;gap:8px;flex-wrap:wrap">
      <div style="font-size:14px;font-weight:800;color:#10192B">📢 ${esc(open.title)}</div>
      ${mockDdayBadge(open.closeAt)}
    </div>
    <div style="font-size:12px;color:#57616F;margin-top:5px">${mockRatioText(open)}</div>
    ${open.mySubmission?`<div style="margin-top:10px;padding:10px 12px;background:#EFF6FF;border:1.5px solid #BFDBFE;border-radius:9px;font-size:13px;font-weight:700;color:#1E40AF">
      ${open.mySubmission.score}점${open.mySubmission.grade?` · ${open.mySubmission.grade}등급`:''} 제출됨 · 마감 전까지 수정할 수 있어요
    </div>`:''}
    ${rMockInputForm(open)}`;
  }else{
    inner=rMockResultCard(last);
  }
  return `<div id="mock-card" style="background:#fff;border:1.5px solid #D9E0EA;border-radius:14px;padding:16px 18px;margin-bottom:14px">
    <div style="font-size:10.5px;font-weight:800;letter-spacing:1.2px;color:#8C93A0;margin-bottom:8px">MOCK EXAM · 모의고사</div>
    ${inner}
  </div>`;
}
async function submitMockScore(roundId,score,grade,msgEl){
  try{
    const res=await fetch('/api/student/mock-submit',{
      method:'POST',credentials:'include',headers:{'Content-Type':'application/json'},
      body:JSON.stringify({roundId,score,grade})
    });
    const data=await res.json().catch(()=>({}));
    if(!res.ok||!data.success){
      if(msgEl){msgEl.textContent=data.message||'저장에 실패했습니다';msgEl.style.color='#C62828';}
      return false;
    }
    await loadStudentMock();
    return true;
  }catch(e){
    if(msgEl){msgEl.textContent='네트워크 오류로 저장하지 못했습니다';msgEl.style.color='#C62828';}
    return false;
  }
}
function bMockCard(){
  const btn=document.getElementById('mock-submit');
  if(!btn)return;
  btn.onclick=async()=>{
    const msg=document.getElementById('mock-msg');
    const scoreEl=document.getElementById('mock-score');
    const gradeEl=document.getElementById('mock-grade');
    const raw=(scoreEl?.value||'').trim();
    if(raw===''){msg.textContent='원점수를 입력하세요';msg.style.color='#C62828';return;}
    const score=Number(raw);
    const max=ST.mock?.openRound?.maxScore??100;
    if(!Number.isInteger(score)||score<0||score>max){
      msg.textContent=`점수는 0~${max} 사이 정수로 입력하세요`;msg.style.color='#C62828';return;
    }
    const grade=gradeEl.value===''?null:Number(gradeEl.value);
    btn.disabled=true;btn.style.opacity='0.6';
    msg.textContent='저장 중...';msg.style.color='#57616F';
    const ok=await submitMockScore(ST.mock.openRound.id,score,grade,msg);
    if(ok)render(); else {btn.disabled=false;btn.style.opacity='1';}
  };
}
```

- [ ] **Step 3: 성적표 탭에 카드 붙이기**

`rStudent()` 안에서 아래 한 줄을 바꾼다. `rReport(recs)`는 성적이 없으면 안내 문구만 반환하므로, 모의고사 카드는 그 바깥에 붙여야 주차 성적이 없는 학생에게도 보인다.

변경 전:
```javascript
  if(ST.tab==='report') inner=rReport(recs);
```
변경 후:
```javascript
  if(ST.tab==='report') inner=rMockCard()+rReport(recs);
```

`bStudent()` 첫 줄 `document.getElementById('btn-lo').onclick=doLogout;` **바로 뒤**에 추가한다.
```javascript
  bMockCard();
```

- [ ] **Step 4: 문법 검사와 수동 확인**

Run:
```bash
node -e "const fs=require('fs');const h=fs.readFileSync('index.html','utf8');const m=[...h.matchAll(/<script>([\s\S]*?)<\/script>/g)];m.forEach(x=>{new Function(x[1]);});console.log('syntax OK, blocks:',m.length)"
npm test
```
Expected: `syntax OK, blocks: 2` 그리고 기존 테스트 전부 통과

수동 확인 (프리뷰 배포 또는 로컬):
1. 관리자로 로그인 → 회차를 하나 만든다(아직 UI가 없으므로 `POST /api/admin/mock-round-save`를 직접 호출)
2. 그 학년 학생으로 로그인 → 성적표 탭 맨 위에 입력 카드가 뜬다
3. 점수 88, 등급 2등급 제출 → 카드가 "88점 · 2등급 제출됨"으로 바뀐다
4. 페이지 새로고침 → 값이 유지된다
5. 주차 성적이 하나도 없는 학생으로도 카드가 보이는지 확인

- [ ] **Step 5: 커밋**

```bash
git add index.html
git commit -m "학생 성적표에 모의고사 입력·결과 카드 추가

성적표 탭 맨 위에 상시 카드를 둔다. 열린 회차가 있으면 입력 폼,
제출했으면 제출값과 수정 버튼, 열린 회차가 없으면 최근 결과 카드.

등급은 자유 입력이 아니라 1~9 드롭다운만 받는다 — 표기가 흔들리면
나중에 필터링이 안 된다. 카드를 rReport() 바깥에 붙여, 주차 성적이
아직 없는 학생에게도 입력칸이 보이게 했다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_015KDw3RTJ9Hurhdt2NhkgTa"
```

---

### Task 6: 학생 화면 — 모의고사 추이 그래프

**Files:**
- Modify: `index.html` — Task 5의 `rMockCard()` 뒤에 그래프 섹션 추가, `bStudent()`에 그리기 호출 추가

**Interfaces:**
- Consumes: Task 5의 `ST.mock.history`, `Chart` 전역 (Chart.js 4.4.1, 23행에서 이미 로드됨)
- Produces:
  - `function rMockTrend(): string` — 그래프 영역 HTML (마감된 기록 2개 미만이면 빈 문자열)
  - `function drawMockCharts(): void`

기존 `mkLine`(1084행)과 **같은 색·굵기·점선 스타일**로 맞추되, 차트를 직접 만든다. 학년 구분선을 그리려면 `plugins` 훅이 필요한데 `mkLine`은 그걸 받지 않기 때문이다. 등수 추이도 y축을 뒤집어야 해서 별도로 만든다.

- [ ] **Step 1: 그래프 함수 추가**

`function bMockCard(){` **바로 위**에 추가한다.

```javascript
// 모의고사 추이 — 기존 성적표의 RT/WT 차트(mkLine)와 같은 스타일로 맞춘다.
// 학년이 바뀌는 지점에 세로 구분선을 그려, 1학년부터 3학년까지 한 그래프에 이어지게 한다.
function rMockTrend(){
  const hist=(ST.mock?.history||[]);
  if(hist.length<2)return'';
  return `
  <div class="no-print" style="margin-bottom:14px">
    <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:10px">
      <div style="font-size:10.5px;font-weight:800;color:#57616F;letter-spacing:1px">📈 모의고사 추이</div>
      <div style="display:flex;gap:10px;align-items:center">
        <span style="display:flex;align-items:center;gap:4px;font-size:11px;color:#57616F;font-weight:700"><span style="width:8px;height:8px;border-radius:50%;background:#1E40AF;display:inline-block"></span>내 점수</span>
        <span style="display:flex;align-items:center;gap:4px;font-size:11px;color:#57616F;font-weight:700"><span style="width:14px;height:0;border-top:2px dashed #8C93A0;display:inline-block"></span>학년 평균</span>
      </div>
    </div>
    <div style="background:#fff;border:1.5px solid #D9E0EA;border-radius:12px;padding:14px;margin-bottom:10px">
      <div style="position:relative;height:160px"><canvas id="mock-trend"></canvas></div>
    </div>
    <div style="background:#fff;border:1.5px solid #D9E0EA;border-radius:12px;padding:14px">
      <div style="font-size:11px;font-weight:800;color:#1E40AF;margin-bottom:10px">학년 등수 추이</div>
      <div style="position:relative;height:140px"><canvas id="mock-rank"></canvas></div>
    </div>
  </div>`;
}
function drawMockCharts(){
  const hist=(ST.mock?.history||[]);
  if(hist.length<2||typeof Chart==='undefined')return;
  const labels=hist.map(h=>h.title.length>10?h.title.slice(0,10)+'…':h.title);
  const scores=hist.map(h=>h.score);
  const avgs=hist.map(h=>h.avg);
  const all=[...scores,...avgs].filter(v=>v!=null);
  const yMin=all.length?Math.max(0,Math.floor(Math.min(...all)/10)*10-10):0;
  const yMax=all.length?Math.min(100,Math.ceil(Math.max(...all)/10)*10+10):100;
  // 학년이 바뀌는 지점의 세로 구분선. 차트를 그린 뒤 캔버스에 직접 그리면 다음 애니메이션
  // 프레임에 지워지므로, Chart.js의 afterDraw 훅에 붙여 매 프레임 다시 그리게 한다.
  const gradeMarks=[];
  hist.forEach((h,i)=>{if(i>0&&h.grade!==hist[i-1].grade)gradeMarks.push({i,label:h.grade});});
  const gradeLinePlugin={
    id:'mockGradeLine',
    afterDraw(chart){
      if(!gradeMarks.length)return;
      const ctx=chart.ctx,area=chart.chartArea;
      gradeMarks.forEach(mk=>{
        const x=chart.scales.x.getPixelForValue(mk.i);
        ctx.save();
        ctx.strokeStyle='#C4CBD6';ctx.lineWidth=1.5;ctx.setLineDash([4,4]);
        ctx.beginPath();ctx.moveTo(x,area.top);ctx.lineTo(x,area.bottom);ctx.stroke();
        ctx.setLineDash([]);
        ctx.fillStyle='#8C93A0';ctx.font='600 10px Pretendard, sans-serif';ctx.textAlign='left';
        ctx.fillText(mk.label+' ▶',x+3,area.top+11);
        ctx.restore();
      });
    }
  };

  const tc=document.getElementById('mock-trend');
  if(tc){
    new Chart(tc,{type:'line',data:{labels,datasets:[
      {label:'내 점수',data:scores,borderColor:'#1E40AF',backgroundColor:'#1E40AF18',borderWidth:3,tension:.35,pointRadius:5,pointBackgroundColor:'#fff',pointBorderColor:'#1E40AF',pointBorderWidth:2.5,fill:true,spanGaps:true},
      {label:'학년 평균',data:avgs,borderColor:'#1E40AF',backgroundColor:'transparent',borderWidth:1.8,borderDash:[7,4],tension:.35,pointRadius:3,pointBackgroundColor:'#1E40AF',fill:false,spanGaps:true},
    ]},options:{responsive:true,maintainAspectRatio:false,
      plugins:{legend:{display:false},tooltip:{callbacks:{label:c=>`${c.dataset.label}: ${c.parsed.y}점`}}},
      scales:{y:{min:yMin,max:yMax,grid:{color:'rgba(128,128,128,.1)'},ticks:{font:{size:10},callback:v=>`${v}점`}},
        x:{grid:{display:false},ticks:{font:{size:10},maxRotation:0}}}},
      plugins:[gradeLinePlugin]});
  }

  // 등수 추이 — 1등이 위로 오도록 y축을 뒤집는다
  const rc=document.getElementById('mock-rank');
  const ranks=hist.map(h=>h.rank);
  if(rc&&ranks.some(r=>r!=null)){
    new Chart(rc,{type:'line',data:{labels,datasets:[{
      label:'학년 등수',data:ranks,borderColor:'#0F2A5C',backgroundColor:'#0F2A5C18',
      borderWidth:3,tension:.35,pointRadius:5,pointBackgroundColor:'#fff',
      pointBorderColor:'#0F2A5C',pointBorderWidth:2.5,fill:false,spanGaps:true,
    }]},options:{responsive:true,maintainAspectRatio:false,
      plugins:{legend:{display:false},tooltip:{callbacks:{label:ctx=>`${ctx.parsed.y}등`}}},
      scales:{y:{reverse:true,min:1,grid:{color:'rgba(128,128,128,.1)'},ticks:{font:{size:10},precision:0,callback:v=>`${v}등`}},
        x:{grid:{display:false},ticks:{font:{size:10},maxRotation:0}}}}});
  }
}
```

- [ ] **Step 2: 성적표에 그래프 붙이기**

`rStudent()`의 report 분기를 바꾼다.

변경 전:
```javascript
  if(ST.tab==='report') inner=rMockCard()+rReport(recs);
```
변경 후:
```javascript
  if(ST.tab==='report') inner=rMockCard()+rMockTrend()+rReport(recs);
```

`bStudent()`의 `bMockCard();` **바로 뒤**에 추가한다. 기존 성적표 차트도 `requestAnimationFrame` 뒤에 그리므로 같은 타이밍에 맞춘다.
```javascript
  requestAnimationFrame(()=>drawMockCharts());
```

- [ ] **Step 3: 문법 검사와 수동 확인**

Run:
```bash
node -e "const fs=require('fs');const h=fs.readFileSync('index.html','utf8');const m=[...h.matchAll(/<script>([\s\S]*?)<\/script>/g)];m.forEach(x=>{new Function(x[1]);});console.log('syntax OK, blocks:',m.length)"
```
Expected: `syntax OK, blocks: 2`

수동 확인:
1. 마감된 회차 2개 이상에 같은 학생의 제출을 넣는다(`POST /api/admin/mock-edit`로 대리 입력 가능)
2. 학생으로 로그인 → 그래프 2개가 그려진다
3. 회차 하나의 학년을 다르게 만들어 두면 세로 구분선과 `2학년 ▶` 라벨이 보인다
4. 기록이 1개뿐인 학생에게는 그래프 영역이 아예 안 나온다

- [ ] **Step 4: 커밋**

```bash
git add index.html
git commit -m "학생 성적표에 모의고사 추이 그래프 추가

내 점수와 학년 평균을 겹쳐 그리고, 학년이 바뀌는 지점에 세로 구분선을
넣어 1학년부터 3학년까지 한 그래프에 이어지게 했다.
등수 추이는 1등이 위로 오도록 y축을 뒤집었다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_015KDw3RTJ9Hurhdt2NhkgTa"
```

---

### Task 7: 학생 화면 — 로그인 직후 입력 팝업

**Files:**
- Modify: `index.html` — Task 5의 함수들 뒤에 팝업 함수 추가, `render()` 뒤에서 호출

**Interfaces:**
- Consumes: `ST.mock.openRound`, Task 5의 `rMockInputForm`, `mockRatioText`, `mockDdayBadge`, `submitMockScore`
- Produces: `function maybeShowMockPopup(): void`

조건: 열린 회차가 있고 **내가 아직 미제출**이며, 이번 세션에서 "나중에"를 누르지 않았을 때만 뜬다. `sessionStorage`를 쓰므로 다음 로그인 때 다시 뜬다.

- [ ] **Step 1: 팝업 함수 추가**

`function bMockCard(){` **바로 위**에 추가한다.

```javascript
// 로그인 직후 1회 — 열린 회차가 있는데 아직 안 낸 학생에게만 뜬다.
// "나중에"는 sessionStorage에만 기록해서 그 세션 동안만 숨긴다. 다음 로그인 때 다시 뜬다.
function mockLaterKey(roundId){return 'oheng_mock_later_'+roundId;}
function maybeShowMockPopup(){
  const open=ST.mock?.openRound;
  if(!open||open.mySubmission)return;
  if(document.querySelector('.mock-popup'))return;
  try{ if(sessionStorage.getItem(mockLaterKey(open.id)))return; }catch(e){}

  const popup=document.createElement('div');
  popup.className='mock-popup';
  popup.style.cssText='position:fixed;inset:0;background:rgba(0,0,0,0.5);z-index:9998;display:flex;align-items:center;justify-content:center;padding:20px';
  popup.innerHTML=`
    <div style="background:#fff;border-radius:18px;max-width:400px;width:100%;padding:22px 22px 18px;box-shadow:0 8px 32px rgba(0,0,0,0.25)">
      <div style="display:flex;justify-content:space-between;align-items:center;gap:8px;flex-wrap:wrap;margin-bottom:6px">
        <div style="font-size:15px;font-weight:800;color:#10192B">📢 ${esc(open.title)}</div>
        ${mockDdayBadge(open.closeAt)}
      </div>
      <div style="font-size:12px;color:#57616F;margin-bottom:2px">${mockRatioText(open)}</div>
      ${rMockInputForm(open)}
      <button id="mock-later" style="width:100%;margin-top:8px;padding:10px;background:transparent;border:none;font-size:13px;font-weight:700;color:#8C93A0;cursor:pointer;font-family:inherit">나중에 할게요</button>
    </div>`;
  document.body.appendChild(popup);

  const close=()=>popup.remove();
  popup.querySelector('#mock-later').onclick=()=>{
    try{ sessionStorage.setItem(mockLaterKey(open.id),'1'); }catch(e){}
    close();
  };
  popup.querySelector('#mock-submit').onclick=async()=>{
    const msg=popup.querySelector('#mock-msg');
    const raw=(popup.querySelector('#mock-score').value||'').trim();
    if(raw===''){msg.textContent='원점수를 입력하세요';msg.style.color='#C62828';return;}
    const score=Number(raw);
    if(!Number.isInteger(score)||score<0||score>open.maxScore){
      msg.textContent=`점수는 0~${open.maxScore} 사이 정수로 입력하세요`;msg.style.color='#C62828';return;
    }
    const gv=popup.querySelector('#mock-grade').value;
    const btn=popup.querySelector('#mock-submit');
    btn.disabled=true;btn.style.opacity='0.6';
    msg.textContent='저장 중...';msg.style.color='#57616F';
    const ok=await submitMockScore(open.id,score,gv===''?null:Number(gv),msg);
    if(ok){close();render();}
    else{btn.disabled=false;btn.style.opacity='1';}
  };
}
```

**주의:** 팝업 안의 `#mock-score`/`#mock-grade`/`#mock-submit`/`#mock-msg`는 성적표 카드와 id가 겹친다. 위 코드는 항상 `popup.querySelector(...)`로 팝업 안쪽만 찾으므로 문제가 없지만, `bMockCard()`의 `document.getElementById('mock-submit')`은 문서 순서상 먼저 나오는 카드 쪽을 잡는다. 팝업은 `document.body`에 append되어 카드보다 뒤에 오므로 카드가 우선 잡힌다 — 의도한 동작이다.

- [ ] **Step 2: 로그인 직후 호출**

`bStudent()`의 `requestAnimationFrame(()=>drawMockCharts());` **바로 뒤**에 추가한다.

```javascript
  maybeShowMockPopup();
```

- [ ] **Step 3: 문법 검사와 수동 확인**

Run:
```bash
node -e "const fs=require('fs');const h=fs.readFileSync('index.html','utf8');const m=[...h.matchAll(/<script>([\s\S]*?)<\/script>/g)];m.forEach(x=>{new Function(x[1]);});console.log('syntax OK, blocks:',m.length)"
```
Expected: `syntax OK, blocks: 2`

수동 확인:
1. 미제출 학생으로 로그인 → 팝업이 뜬다
2. `나중에 할게요` → 닫히고, 탭을 옮겨도 다시 안 뜬다
3. 브라우저 탭을 닫았다 새로 열어 로그인 → 다시 뜬다
4. 팝업에서 제출 → 닫히고 성적표 카드가 "제출됨" 상태로 바뀐다
5. 이미 제출한 학생으로 로그인 → 팝업이 안 뜬다

- [ ] **Step 4: 커밋**

```bash
git add index.html
git commit -m "학생 로그인 직후 모의고사 입력 팝업 추가

열린 회차가 있는데 아직 안 낸 학생에게만 세션당 1회 뜬다.
'나중에'는 sessionStorage에만 기록해 그 세션 동안만 숨기고,
다음 로그인 때 다시 뜨게 했다 — 한 번 닫으면 영영 못 찾는 걸 막기 위함.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_015KDw3RTJ9Hurhdt2NhkgTa"
```

---

### Task 8: 선생님 화면 — 반 사이드바 `모의고사` 탭

**Files:**
- Modify: `index.html` — `rAdmin()`의 `menuGroups`(2837행 근처)와 분기, 새 렌더·바인딩 함수, `bAdmin()`

**Interfaces:**
- Consumes: `GET /api/admin/mock-rounds?grade=`, `GET /api/admin/mock-detail?roundId=&schoolId=`, `POST /api/admin/mock-edit`
- Produces:
  - `ST.mockAdmin: { rounds, roundId, detail } | null`
  - `async function loadMockAdmin(sc, roundId): Promise<void>`
  - `function rMockAdmin(sc): string`
  - `function bMockAdmin(sc): void`

- [ ] **Step 1: 메뉴 항목과 분기 추가**

`rAdmin()`의 `menuGroups` 중 `업무` 그룹에서 `{id:'records',l:'성적 일괄 입력',icon:'📝'},` **바로 뒤**에 추가한다.
```javascript
      {id:'mock',l:'모의고사',icon:'📈'},
```

같은 함수의 분기에서 `if(ST.atab==='records') inner=rBulk(sc);` **바로 뒤**에 추가한다.
```javascript
  else if(ST.atab==='mock') inner=rMockAdmin(sc);
```

- [ ] **Step 2: 렌더·바인딩 함수 추가**

`function rDashboard(sc){` **바로 위**에 추가한다.

```javascript
// 선생님용 모의고사 탭 — 회차를 고르면 우리 반 제출 현황과 학년 등수를 함께 본다.
// 등수는 학년 전체 기준(mock-detail의 aggregate.rankOf)이고, 표는 우리 반만 보여준다.
async function loadMockAdmin(sc,roundId){
  try{
    const listRes=await fetch(`/api/admin/mock-rounds?grade=${encodeURIComponent(sc.grade||'')}`,{credentials:'include'});
    const listData=listRes.ok?await listRes.json():{rounds:[]};
    const rounds=listData.rounds||[];
    const pick=roundId||rounds[0]?.id||null;
    let detail=null;
    if(pick){
      const dRes=await fetch(`/api/admin/mock-detail?roundId=${encodeURIComponent(pick)}&schoolId=${encodeURIComponent(sc.id)}`,{credentials:'include'});
      if(dRes.ok){const d=await dRes.json();if(d.success)detail=d;}
    }
    ST.mockAdmin={rounds,roundId:pick,detail};
  }catch(e){ST.mockAdmin={rounds:[],roundId:null,detail:null};}
}
function rMockAdmin(sc){
  const m=ST.mockAdmin;
  if(!m){loadMockAdmin(sc,null).then(()=>render());return`<div class="section-card"><div style="color:#9BA3AF;font-size:13px">불러오는 중...</div></div>`;}
  if(!m.rounds.length){
    return`<div class="section-card">
      <div style="color:#9BA3AF;font-size:13px;line-height:1.7">
        <b>${esc(sc.grade)}</b>에 개설된 모의고사 회차가 없습니다.<br>
        학교 선택 화면의 <b>📈 모의고사 회차 관리</b>에서 회차를 먼저 만들어 주세요.
      </div></div>`;
  }
  const d=m.detail;
  const agg=d?.aggregate||{count:0,avg:null,max:null,rankOf:{},bySchool:{}};
  const subs=d?.submissions||[];
  const missing=d?.missing||[];
  const total=subs.length+missing.length;
  const mine=agg.bySchool?.[sc.id]||null;
  const g1=subs.filter(s=>s.grade===1).length;

  const rows=subs.map(s=>`
    <tr>
      <td style="font-weight:700">${esc(s.name)}</td>
      <td style="text-align:center;font-weight:800;color:#1E40AF">${s.score}</td>
      <td style="text-align:center">${s.grade?s.grade+'등급':'-'}</td>
      <td style="text-align:center;font-weight:700">${s.excluded?'<span style="color:#B3261E">제외</span>':(agg.rankOf?.[s.sid]??'-')}</td>
      <td style="text-align:center;font-size:11px;color:#9BA3AF">${s.updatedAt?new Date(s.updatedAt).toLocaleDateString('ko-KR'):'-'}</td>
      <td style="text-align:center;white-space:nowrap">
        <button class="abtn" data-mock-edit="${esc(s.sid)}" title="점수 수정">✏️</button>
        <button class="abtn" data-mock-excl="${esc(s.sid)}" data-cur="${s.excluded?'1':'0'}" title="${s.excluded?'집계에 다시 포함':'집계에서 제외'}">${s.excluded?'↩️':'🚫'}</button>
      </td>
    </tr>`).join('');
  const missRows=missing.map(s=>`
    <tr style="background:#FAFBFC;color:#9BA3AF">
      <td style="font-weight:700">${esc(s.name)}</td>
      <td colspan="4" style="text-align:center;font-size:11.5px;font-weight:700">미제출</td>
      <td style="text-align:center"><button class="abtn" data-mock-add="${esc(s.id)}" title="선생님이 대신 입력">➕</button></td>
    </tr>`).join('');

  return`
  <div class="section-card">
    <div style="display:flex;gap:10px;align-items:center;flex-wrap:wrap;margin-bottom:14px">
      <select id="mock-round-pick" class="finp" style="max-width:280px">
        ${m.rounds.map(r=>`<option value="${esc(r.id)}" ${r.id===m.roundId?'selected':''}>${esc(r.title)} (${esc(r.examDate)})</option>`).join('')}
      </select>
    </div>
    <div style="background:#EFF6FF;border:1.5px solid #BFDBFE;border-radius:10px;padding:12px 14px;font-size:13px;font-weight:700;color:#1E40AF;line-height:1.7">
      우리 반 ${total}명 중 <b>${subs.length}명</b> 입력
      ${mine?` · 평균 ${mine.avg} (학년 ${agg.avg ?? '-'}) · 최고 ${mine.max}`:''}
      · 1등급 ${g1}명
    </div>
  </div>
  <div class="section-card">
    <div class="section-ttl">📋 우리 반 결과</div>
    <table class="tbl" style="width:100%">
      <thead><tr>
        <th style="text-align:left">이름</th><th>원점수</th><th>등급</th><th>학년 등수</th><th>제출</th><th></th>
      </tr></thead>
      <tbody>${rows}${missRows}</tbody>
    </table>
  </div>
  <div class="section-card">
    <div class="section-ttl">📈 회차별 우리 반 vs 학년 평균</div>
    <div style="position:relative;height:190px"><canvas id="mock-admin-trend"></canvas></div>
  </div>
  <div class="section-card">
    <div class="section-ttl">📊 이번 회차 반별 평균</div>
    <div style="position:relative;height:190px"><canvas id="mock-admin-bars"></canvas></div>
  </div>`;
}
function bMockAdmin(sc){
  const pick=document.getElementById('mock-round-pick');
  if(!pick)return;
  pick.onchange=async()=>{await loadMockAdmin(sc,pick.value);render();};

  const edit=async(sid,body)=>{
    const res=await fetch('/api/admin/mock-edit',{
      method:'POST',credentials:'include',headers:{'Content-Type':'application/json'},
      body:JSON.stringify({roundId:ST.mockAdmin.roundId,sid,...body})
    });
    const data=await res.json().catch(()=>({}));
    if(!res.ok||!data.success){alert(data.message||'저장에 실패했습니다');return;}
    await loadMockAdmin(sc,ST.mockAdmin.roundId);render();
  };
  document.querySelectorAll('[data-mock-edit]').forEach(b=>{
    b.onclick=()=>{
      const sid=b.dataset.mockEdit;
      const cur=(ST.mockAdmin.detail?.submissions||[]).find(s=>s.sid===sid);
      const sv=prompt(`${cur?.name||''} 원점수`,cur?.score??'');
      if(sv===null)return;
      const gv=prompt(`${cur?.name||''} 등급 (1~9, 없으면 비워두세요)`,cur?.grade??'');
      if(gv===null)return;
      edit(sid,{score:Number(sv),grade:gv===''?null:Number(gv)});
    };
  });
  document.querySelectorAll('[data-mock-excl]').forEach(b=>{
    b.onclick=()=>{
      const sid=b.dataset.mockExcl;
      const on=b.dataset.cur==='1';
      if(on){edit(sid,{excluded:false});return;}
      const memo=prompt('집계에서 제외하는 사유 (선택)','');
      if(memo===null)return;
      edit(sid,{excluded:true,memo});
    };
  });
  document.querySelectorAll('[data-mock-add]').forEach(b=>{
    b.onclick=()=>{
      const sid=b.dataset.mockAdd;
      const who=(ST.mockAdmin.detail?.missing||[]).find(s=>s.id===sid);
      const sv=prompt(`${who?.name||''} 원점수 (선생님 대신 입력)`,'');
      if(sv===null||sv==='')return;
      const gv=prompt(`${who?.name||''} 등급 (1~9, 없으면 비워두세요)`,'');
      if(gv===null)return;
      edit(sid,{schoolId:sc.id,score:Number(sv),grade:gv===''?null:Number(gv)});
    };
  });

  // 그래프 2개
  requestAnimationFrame(async()=>{
    if(typeof Chart==='undefined')return;
    const m=ST.mockAdmin;
    if(!m||!m.rounds.length)return;

    // ① 회차별 우리 반 평균 vs 학년 평균 — 회차마다 집계를 받아온다(회차 수는 많아야 수십 개)
    const details=await Promise.all(m.rounds.slice().reverse().map(async r=>{
      const res=await fetch(`/api/admin/mock-detail?roundId=${encodeURIComponent(r.id)}&schoolId=${encodeURIComponent(sc.id)}`,{credentials:'include'});
      if(!res.ok)return null;
      const d=await res.json();
      return d.success?{title:r.title,agg:d.aggregate}:null;
    }));
    const ok=details.filter(Boolean);
    if(ok.length){
      mkLine('mock-admin-trend',
        ok.map(x=>x.title.length>10?x.title.slice(0,10)+'…':x.title),
        ok.map(x=>x.agg.bySchool?.[sc.id]?.avg??null),
        ok.map(x=>x.agg.avg??null),
        '#1E40AF',0,100);
    }

    // ② 이번 회차 반별 평균 — 우리 반만 강조
    const bs=m.detail?.aggregate?.bySchool||{};
    const keys=Object.keys(bs);
    const bars=document.getElementById('mock-admin-bars');
    if(bars&&keys.length){
      new Chart(bars,{type:'bar',data:{
        labels:keys.map(k=>bs[k].schoolName||k),
        datasets:[{data:keys.map(k=>bs[k].avg),
          backgroundColor:keys.map(k=>k===sc.id?'#1E40AF':'#C4CBD6'),borderRadius:6}]
      },options:{responsive:true,maintainAspectRatio:false,
        plugins:{legend:{display:false},tooltip:{callbacks:{label:ctx=>`평균 ${ctx.parsed.y}점`}}},
        scales:{y:{beginAtZero:true,grid:{color:'rgba(128,128,128,.1)'},ticks:{font:{size:10}}},
          x:{grid:{display:false},ticks:{font:{size:10},maxRotation:0}}}}});
    }
  });
}
```

- [ ] **Step 3: `bAdmin()`에 바인딩 연결**

`bAdmin()` 안에서 다른 탭 바인딩과 같은 위치에 추가한다. `ST.atab` 분기를 찾아 아래를 넣는다.
```javascript
  if(ST.atab==='mock') bMockAdmin(curSch());
```

탭을 떠날 때 캐시가 남지 않도록, `rAdmin()`의 `ST._lastAtab=ST.atab;` **바로 앞**에 추가한다.
```javascript
  if(ST.atab!=='mock')ST.mockAdmin=null;
```

- [ ] **Step 4: 문법 검사와 수동 확인**

Run:
```bash
node -e "const fs=require('fs');const h=fs.readFileSync('index.html','utf8');const m=[...h.matchAll(/<script>([\s\S]*?)<\/script>/g)];m.forEach(x=>{new Function(x[1]);});console.log('syntax OK, blocks:',m.length)"
npm test
```
Expected: `syntax OK, blocks: 2`, 테스트 전부 통과

수동 확인:
1. 반에 들어가 사이드바 `📈 모의고사` → 회차 드롭다운이 뜬다
2. 제출한 학생은 점수·등급·**학년 등수**가, 안 낸 학생은 회색 `미제출` 행이 보인다
3. ✏️로 점수를 고치면 등수와 요약 배너가 바로 갱신된다
4. 🚫로 제외하면 그 학생이 집계에서 빠지고 평균이 바뀐다
5. ➕로 미제출자를 대신 입력하면 행이 정상 행으로 바뀐다
6. 회차가 2개 이상이면 추이 그래프가, 반이 2개 이상이면 반별 막대가 그려진다
7. 회차가 없는 학년의 반에서는 안내 문구가 뜬다

- [ ] **Step 5: 커밋**

```bash
git add index.html
git commit -m "선생님 반 화면에 모의고사 탭 추가

회차를 고르면 우리 반 제출 현황·점수·학년 등수를 표로 보고,
회차별 우리 반 평균 vs 학년 평균, 이번 회차 반별 평균을 그래프로 본다.

미제출자는 회색 행으로 함께 보여주고 ➕로 선생님이 대신 입력할 수 있게 했다.
점수 수정·집계 제외도 이 표에서 바로 처리한다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_015KDw3RTJ9Hurhdt2NhkgTa"
```

---

### Task 9: 선생님 화면 — 미제출자 명단 복사와 학생별 이력 모달

**Files:**
- Modify: `index.html` — Task 8의 `rMockAdmin(sc)` / `bMockAdmin(sc)` 확장

**Interfaces:**
- Consumes: Task 8의 `ST.mockAdmin`, `GET /api/admin/mock-detail`
- Produces:
  - `async function openMockHistoryModal(sc, sid): Promise<void>`

**독촉 발송에 대해:** spec 6.2는 "기존 뿌리오 발송으로 독촉"이라고 썼지만, 실제 코드를 보니 `showPpurioAllPopup()`은 **통신사에 사전 등록된 알림톡 템플릿**(`PPURIO_TPL_ACCOUNT` + `PPURIO_PFID_ACCOUNT`)에 묶여 있다. 모의고사 독촉 문구를 보내려면 새 템플릿을 통신사에 등록해야 하는데, 그건 코드 밖의 일이라 이 계획에서 할 수 없다. 계정 안내 템플릿을 그대로 재활용하면 학생에게 엉뚱한 문자가 간다.

그래서 이 태스크는 **미제출자 명단 복사**만 넣는다. 선생님이 버튼 하나로 명단을 복사해 반 단톡방에 붙여넣을 수 있고, 외부 의존이 전혀 없다. 알림톡 템플릿을 등록하시면 그때 실제 발송 버튼을 별도 건으로 붙인다.

- [ ] **Step 1: 미제출자 명단 복사 버튼 추가**

`rMockAdmin(sc)`의 "우리 반 결과" 섹션 제목 줄을 바꾼다.

변경 전:
```javascript
    <div class="section-ttl">📋 우리 반 결과</div>
```
변경 후:
```javascript
    <div class="section-ttl" style="display:flex;justify-content:space-between;align-items:center;flex-wrap:wrap;gap:8px">
      <span>📋 우리 반 결과</span>
      ${missing.length?`<button class="abtn abtn-gray" id="mock-copy-missing" style="font-size:11px;padding:6px 12px">📋 미제출 ${missing.length}명 명단 복사</button>`:''}
    </div>
```

`bMockAdmin(sc)`의 `pick.onchange=...` 바로 뒤에 추가한다.

```javascript
  // 알림톡 템플릿은 통신사 사전 등록이 필요해 자동 발송은 붙일 수 없다.
  // 명단을 복사해 반 단톡방에 붙여넣는 쪽이 지금 당장 쓸 수 있는 방법이다.
  const copyBtn=document.getElementById('mock-copy-missing');
  if(copyBtn){
    copyBtn.onclick=async()=>{
      const miss=ST.mockAdmin.detail?.missing||[];
      const r=ST.mockAdmin.rounds.find(x=>x.id===ST.mockAdmin.roundId);
      const due=r?`${msToYmd(r.closeAt)}까지`:'';
      const text=`[${r?r.title:'모의고사'}] 점수 미입력 ${miss.length}명 ${due}\n`+miss.map(s=>s.name).join(', ');
      try{
        await navigator.clipboard.writeText(text);
        copyBtn.textContent='✓ 복사됨';
        setTimeout(()=>{copyBtn.textContent=`📋 미제출 ${miss.length}명 명단 복사`;},1500);
      }catch(e){
        prompt('아래 명단을 복사하세요', text);
      }
    };
  }
```

`msToYmd()`는 Task 10에서 정의하는데 이 태스크가 먼저 실행된다. **`msToYmd`와 `ymdToMs`를 이 태스크에서 먼저 추가하고, Task 10에서는 중복 정의하지 않는다.** `function rMockAdmin(sc){` 바로 위에 넣는다.

```javascript
function ymdToMs(s,endOfDay){
  const m=/^(\d{4})[.\-/](\d{2})[.\-/](\d{2})$/.exec(String(s||'').trim());
  if(!m)return NaN;
  return new Date(+m[1],+m[2]-1,+m[3],endOfDay?23:0,endOfDay?59:0,endOfDay?59:0).getTime();
}
function msToYmd(ms){
  if(!ms)return'';
  const d=new Date(ms);
  return `${d.getFullYear()}.${String(d.getMonth()+1).padStart(2,'0')}.${String(d.getDate()).padStart(2,'0')}`;
}
```

- [ ] **Step 2: 학생 이름 클릭 → 전 학년 이력 모달**

`rMockAdmin(sc)`의 제출 행에서 이름 칸을 클릭 가능하게 바꾼다.

변경 전:
```javascript
      <td style="font-weight:700">${esc(s.name)}</td>
      <td style="text-align:center;font-weight:800;color:#1E40AF">${s.score}</td>
```
변경 후:
```javascript
      <td style="font-weight:700"><span data-mock-hist="${esc(s.sid)}" style="cursor:pointer;color:#1E40AF;text-decoration:underline dotted" title="전체 회차 이력 보기">${esc(s.name)}</span></td>
      <td style="text-align:center;font-weight:800;color:#1E40AF">${s.score}</td>
```

`bMockAdmin(sc)` 안, 그래프 `requestAnimationFrame` 블록 **앞**에 추가한다.

```javascript
  document.querySelectorAll('[data-mock-hist]').forEach(el=>{
    el.onclick=()=>openMockHistoryModal(sc,el.dataset.mockHist);
  });
```

`function bMockAdmin(sc){` 바로 위에 모달 함수를 추가한다.

```javascript
// 학생 한 명의 전체 회차 이력 — 학년이 올라가도 같은 학생 ID로 묶여 있어 쭉 이어진다.
// 회차마다 상세를 받아 그 학생 행만 추린다(회차 수는 많아야 수십 개).
// schoolId로 좁히는 이유는 미제출자 계산 비용 때문 — 진급은 반의 학년만 바꾸는 방식이라
// 같은 학생의 과거 제출도 같은 schoolId를 갖는다.
async function openMockHistoryModal(sc,sid){
  const m=ST.mockAdmin;
  if(!m)return;
  const name=(m.detail?.submissions||[]).find(s=>s.sid===sid)?.name||sid;

  const popup=document.createElement('div');
  popup.className='mock-hist-popup';
  popup.style.cssText='position:fixed;inset:0;background:rgba(0,0,0,0.5);z-index:9999;display:flex;align-items:center;justify-content:center;padding:20px';
  popup.innerHTML=`
    <div style="background:#fff;border-radius:18px;max-width:560px;width:100%;max-height:85vh;overflow-y:auto;box-shadow:0 8px 32px rgba(0,0,0,0.25)">
      <div style="position:sticky;top:0;background:#fff;padding:14px 18px;display:flex;justify-content:space-between;align-items:center;border-bottom:1.5px solid #E8ECF0;border-radius:18px 18px 0 0">
        <div style="font-size:14px;font-weight:800;color:#1A237E">📈 ${esc(name)} 학생 모의고사 이력</div>
        <button class="btn-close-hist" style="background:none;border:none;font-size:20px;cursor:pointer;color:#9BA3AF">✕</button>
      </div>
      <div id="mock-hist-body" style="padding:16px 18px;font-size:13px;color:#9BA3AF">불러오는 중...</div>
    </div>`;
  document.body.appendChild(popup);
  const close=()=>popup.remove();
  popup.querySelector('.btn-close-hist').onclick=close;
  popup.onclick=e=>{if(e.target===popup)close();};

  const results=await Promise.all(m.rounds.slice().reverse().map(async r=>{
    const res=await fetch(`/api/admin/mock-detail?roundId=${encodeURIComponent(r.id)}&schoolId=${encodeURIComponent(sc.id)}`,{credentials:'include'});
    if(!res.ok)return null;
    const d=await res.json();
    if(!d.success)return null;
    const mine=(d.submissions||[]).find(x=>x.sid===sid);
    if(!mine)return null;
    return {title:r.title,examDate:r.examDate,grade:r.grade,score:mine.score,g:mine.grade,
      excluded:mine.excluded,rank:d.aggregate.rankOf?.[sid]??null,count:d.aggregate.count,avg:d.aggregate.avg};
  }));
  const rows=results.filter(Boolean);
  const body=popup.querySelector('#mock-hist-body');
  if(!rows.length){body.textContent='이 학생의 모의고사 기록이 없습니다.';return;}
  body.style.color='#10192B';
  body.innerHTML=`
    <table class="tbl" style="width:100%">
      <thead><tr><th style="text-align:left">회차</th><th>학년</th><th>점수</th><th>등급</th><th>학년 등수</th><th>학년 평균</th></tr></thead>
      <tbody>${rows.map(x=>`<tr>
        <td style="font-weight:700">${esc(x.title)}<div style="font-size:11px;color:#9BA3AF;font-weight:600">${esc(x.examDate)}</div></td>
        <td style="text-align:center">${esc(x.grade)}</td>
        <td style="text-align:center;font-weight:800;color:#1E40AF">${x.score}</td>
        <td style="text-align:center">${x.g?x.g+'등급':'-'}</td>
        <td style="text-align:center;font-weight:700">${x.excluded?'<span style="color:#B3261E">제외</span>':(x.rank!=null?`${x.rank}/${x.count}`:'-')}</td>
        <td style="text-align:center;color:#57616F">${x.avg??'-'}</td>
      </tr>`).join('')}</tbody>
    </table>`;
}
```

- [ ] **Step 3: 문법 검사와 수동 확인**

Run:
```bash
node -e "const fs=require('fs');const h=fs.readFileSync('index.html','utf8');const m=[...h.matchAll(/<script>([\s\S]*?)<\/script>/g)];m.forEach(x=>{new Function(x[1]);});console.log('syntax OK, blocks:',m.length)"
npm test
```
Expected: `syntax OK, blocks: 2`, 테스트 전부 통과

수동 확인:
1. 모의고사 탭에 미제출자가 있으면 `📋 미제출 N명 명단 복사` 버튼이 보인다
2. 누르면 `✓ 복사됨`으로 바뀌고, 붙여넣으면 회차명·마감일·이름 목록이 나온다
3. 클립보드 권한이 없는 환경에서는 복사용 prompt가 대신 뜬다
4. 학생 이름을 누르면 이력 모달이 뜨고, 회차별 점수·등급·학년 등수가 보인다
5. 기록이 2개 이상인 학생은 여러 줄이, 없는 학생은 안내 문구가 뜬다

- [ ] **Step 4: 커밋**

```bash
git add index.html
git commit -m "모의고사 탭에 미제출자 명단 복사와 학생별 이력 모달 추가

알림톡 자동 독촉은 통신사에 새 템플릿을 등록해야 해서 코드만으로 안 된다
(기존 발송은 계정 안내 템플릿에 묶여 있어 재활용하면 엉뚱한 문자가 간다).
대신 미제출자 명단을 한 번에 복사해 단톡방에 붙여넣을 수 있게 했다.

학생 이름을 누르면 그 학생의 전체 회차 이력이 뜬다 — 학생 ID로 묶여 있어
학년이 올라가도 1학년 기록부터 쭉 이어진다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_015KDw3RTJ9Hurhdt2NhkgTa"
```

---

### Task 10: 선생님 화면 — 회차 관리 (학교 선택 화면)

**Files:**
- Modify: `index.html` — `render()`의 admin 분기(1244행 근처), `rSchoolSelect()`에 진입 버튼, 새 화면 함수

**Interfaces:**
- Consumes: `GET /api/admin/mock-rounds`, `POST /api/admin/mock-round-save`, `POST /api/admin/mock-round-delete`, `ST.isMaster`
- Produces:
  - `ST.adminView === 'rounds'` 진입점
  - `ST.roundMgr: { rounds, grades } | null`
  - `async function loadRoundMgr(): Promise<void>`
  - `function rRoundManager(): string`
  - `function bRoundManager(): void`

회차는 학년 단위(여러 반 공통)라 특정 반 안에 두면 어색하므로 학원 전체 레벨에 둔다. 개설·수정·삭제 버튼은 마스터에게만 보이고, 서버도 마스터만 통과시킨다(Task 2).

- [ ] **Step 1: 라우팅과 진입 버튼**

`render()`의 admin 분기에서 `if(ST.adminView==='account'){...}` **바로 뒤**에 추가한다.
```javascript
    if(ST.adminView==='rounds'){app.innerHTML=rRoundManager();bRoundManager();return;}
```

`rSchoolSelect()`의 `<div class="school-sub">수업할 학교를 선택하면...` 이 있는 헤더 영역 안, 그 `div` **바로 뒤**에 버튼을 추가한다.
```javascript
    <button id="btn-round-mgr" style="margin-top:10px;padding:8px 14px;background:#EFF6FF;border:1.5px solid #BFDBFE;border-radius:9px;font-size:12.5px;font-weight:700;color:#1E40AF;cursor:pointer;font-family:inherit">📈 모의고사 회차 관리</button>
```

`bSchoolSelect()` 안에 추가한다.
```javascript
  const rmBtn=document.getElementById('btn-round-mgr');
  if(rmBtn)rmBtn.onclick=()=>{ST.adminView='rounds';ST.roundMgr=null;render();};
```

- [ ] **Step 2: 화면 함수 추가**

`function rSchoolSelect(){` **바로 위**에 추가한다.

```javascript
// 모의고사 회차 관리 — 회차는 학년 단위(여러 반 공통)라 학원 전체 레벨에 둔다.
// 개설·수정·삭제는 마스터만. 학년은 자유 입력이 아니라 현존 학년 드롭다운에서 고른다
// ('1학년'과 '고1'이 섞이면 등수가 두 덩어리로 갈라진다).
async function loadRoundMgr(){
  try{
    const res=await fetch('/api/admin/mock-rounds',{credentials:'include'});
    if(!res.ok){ST.roundMgr={rounds:[],grades:[]};return;}
    const d=await res.json();
    ST.roundMgr={rounds:d.rounds||[],grades:d.grades||[]};
  }catch(e){ST.roundMgr={rounds:[],grades:[]};}
}
// ymdToMs / msToYmd 는 Task 9에서 이미 추가했다 — 다시 정의하지 않는다
function rRoundManager(){
  const m=ST.roundMgr;
  if(!m){loadRoundMgr().then(()=>render());return`<div style="padding:40px;text-align:center;color:#9BA3AF">불러오는 중...</div>`;}
  const master=!!ST.isMaster;
  const rows=m.rounds.map(r=>{
    const pct=r.totalCount?Math.round(r.submittedCount/r.totalCount*100):0;
    const open=Date.now()>=r.openAt&&Date.now()<r.closeAt;
    return`<tr>
      <td style="font-weight:700">${esc(r.title)}</td>
      <td style="text-align:center">${esc(r.grade)}</td>
      <td style="text-align:center">${esc(r.examDate)}</td>
      <td style="text-align:center;font-size:11.5px;color:#57616F">${msToYmd(r.closeAt)}까지 ${open?'<span style="color:#146C43;font-weight:800">입력중</span>':'<span style="color:#8C93A0">마감</span>'}</td>
      <td style="text-align:center;font-weight:800;color:#1E40AF">${r.submittedCount}/${r.totalCount} <span style="font-size:11px;color:#8C93A0">(${pct}%)</span></td>
      <td style="text-align:center;white-space:nowrap">${master?`<button class="abtn" data-round-del="${esc(r.id)}" data-cnt="${r.submittedCount}" data-title="${esc(r.title)}" title="회차 삭제">🗑️</button>`:''}</td>
    </tr>`;
  }).join('');

  const today=msToYmd(Date.now());
  return`
  <div class="top-nav">
    <div class="nav-logo"><img src="/assets/logo-symbol-white.png" alt="OHENG" style="height:22px;width:auto;display:block"></div>
    <div class="nav-right"><button class="nbtn nbtn-school" id="btn-rm-back">← 학교 선택</button></div>
  </div>
  <div style="max-width:920px;margin:0 auto;padding:22px 18px">
    <div style="font-size:20px;font-weight:800;color:#1A237E;margin-bottom:4px">📈 모의고사 회차 관리</div>
    <div style="font-size:12.5px;color:#9BA3AF;margin-bottom:18px">회차는 학년 단위입니다. 회차를 열면 그 학년 학생 전원의 앱에 입력창이 뜹니다.</div>

    ${master?`
    <div class="section-card">
      <div class="section-ttl">➕ 회차 만들기</div>
      <div class="form3" style="margin-bottom:12px">
        <div class="fgrp"><label class="flbl">학년</label>
          <select class="finp" id="rm-grade">${m.grades.map(g=>`<option value="${esc(g)}">${esc(g)}</option>`).join('')}</select>
        </div>
        <div class="fgrp"><label class="flbl">회차 제목</label><input class="finp" id="rm-title" placeholder="예: 2026년 3월 학력평가"></div>
        <div class="fgrp"><label class="flbl">응시일</label><input class="finp" id="rm-exam" placeholder="${today}" value="${today}"></div>
        <div class="fgrp"><label class="flbl">입력 시작일</label><input class="finp" id="rm-open" value="${today}"></div>
        <div class="fgrp"><label class="flbl">입력 마감일</label><input class="finp" id="rm-close" placeholder="${today}"></div>
        <div class="fgrp"><label class="flbl">만점</label><input class="finp" id="rm-max" type="number" value="100"></div>
      </div>
      <button class="abtn abtn-green" id="rm-save">회차 만들기</button>
      <div id="rm-msg" style="font-size:12px;margin-top:8px;min-height:16px"></div>
      ${m.grades.length?'':`<div style="font-size:12px;color:#B45309;margin-top:8px">등록된 학년이 없습니다. 학교(반)를 먼저 만들어 주세요.</div>`}
    </div>`:`
    <div class="section-card"><div style="font-size:12.5px;color:#9BA3AF">회차 개설·삭제는 원장님 계정만 가능합니다. 아래 목록은 조회만 됩니다.</div></div>`}

    <div class="section-card">
      <div class="section-ttl">📋 회차 목록 (${m.rounds.length})</div>
      ${m.rounds.length?`<table class="tbl" style="width:100%">
        <thead><tr><th style="text-align:left">제목</th><th>학년</th><th>응시일</th><th>입력 기간</th><th>제출률</th><th></th></tr></thead>
        <tbody>${rows}</tbody>
      </table>`:`<div style="color:#9BA3AF;font-size:13px">아직 만든 회차가 없습니다.</div>`}
    </div>
  </div>`;
}
function bRoundManager(){
  document.getElementById('btn-rm-back').onclick=()=>{ST.adminView=null;ST.roundMgr=null;render();};

  const save=document.getElementById('rm-save');
  if(save){
    save.onclick=async()=>{
      const msg=document.getElementById('rm-msg');
      const grade=document.getElementById('rm-grade').value;
      const title=document.getElementById('rm-title').value.trim();
      const examDate=document.getElementById('rm-exam').value.trim();
      const openAt=ymdToMs(document.getElementById('rm-open').value,false);
      const closeAt=ymdToMs(document.getElementById('rm-close').value,true);
      const maxScore=Number(document.getElementById('rm-max').value)||100;
      if(!grade){msg.textContent='학년을 고르세요';msg.style.color='#C62828';return;}
      if(!title){msg.textContent='회차 제목을 입력하세요';msg.style.color='#C62828';return;}
      if(!/^\d{4}\.\d{2}\.\d{2}$/.test(examDate)){msg.textContent='응시일은 2026.03.26 형식으로 입력하세요';msg.style.color='#C62828';return;}
      if(!Number.isFinite(openAt)||!Number.isFinite(closeAt)){msg.textContent='입력 시작일·마감일을 2026.03.26 형식으로 입력하세요';msg.style.color='#C62828';return;}
      if(closeAt<=openAt){msg.textContent='마감일은 시작일보다 뒤여야 합니다';msg.style.color='#C62828';return;}

      save.disabled=true;msg.textContent='저장 중...';msg.style.color='#57616F';
      try{
        const res=await fetch('/api/admin/mock-round-save',{
          method:'POST',credentials:'include',headers:{'Content-Type':'application/json'},
          body:JSON.stringify({grade,title,examDate,openAt,closeAt,maxScore})
        });
        const data=await res.json().catch(()=>({}));
        if(!res.ok||!data.success){msg.textContent=data.message||'저장에 실패했습니다';msg.style.color='#C62828';save.disabled=false;return;}
        await loadRoundMgr();render();
      }catch(e){msg.textContent='네트워크 오류로 저장하지 못했습니다';msg.style.color='#C62828';save.disabled=false;}
    };
  }

  document.querySelectorAll('[data-round-del]').forEach(b=>{
    b.onclick=async()=>{
      const id=b.dataset.roundDel;
      const cnt=b.dataset.cnt;
      if(!confirm(`[${b.dataset.title}] 회차를 삭제할까요?\n\n제출된 점수 ${cnt}건이 함께 삭제되며 되돌릴 수 없습니다.`))return;
      b.disabled=true;
      const res=await fetch('/api/admin/mock-round-delete',{
        method:'POST',credentials:'include',headers:{'Content-Type':'application/json'},
        body:JSON.stringify({id})
      });
      const data=await res.json().catch(()=>({}));
      if(!res.ok||!data.success){alert(data.message||'삭제에 실패했습니다');b.disabled=false;return;}
      await loadRoundMgr();render();
    };
  });
}
```

- [ ] **Step 3: 문법 검사와 수동 확인**

Run:
```bash
node -e "const fs=require('fs');const h=fs.readFileSync('index.html','utf8');const m=[...h.matchAll(/<script>([\s\S]*?)<\/script>/g)];m.forEach(x=>{new Function(x[1]);});console.log('syntax OK, blocks:',m.length)"
npm test
```
Expected: `syntax OK, blocks: 2`, 테스트 전부 통과

수동 확인:
1. 원장 계정 → 학교 선택 화면에 `📈 모의고사 회차 관리` 버튼이 보인다
2. 학년 드롭다운에 현존 학년만 나온다 (직접 타이핑 불가)
3. 회차를 만들면 목록에 `0/N (0%)`로 뜬다
4. 그 학년 학생이 제출하면 제출률이 올라간다
5. 🗑️ 삭제 확인창에 **제출 건수가 명시**된다
6. 조교 계정으로 들어가면 개설 폼이 없고 목록만 보인다
7. 조교 계정에서 `POST /api/admin/mock-round-save`를 직접 호출하면 403이 돌아온다

- [ ] **Step 4: 커밋**

```bash
git add index.html
git commit -m "모의고사 회차 관리 화면 추가 (학교 선택 화면 진입)

회차는 학년 단위라 특정 반 안이 아니라 학원 전체 레벨에 두었다.
학년은 현존 학년 드롭다운에서만 고르게 해서 '1학년'과 '고1'이 섞여
등수가 두 덩어리로 갈라지는 사고를 막는다.

개설·삭제 버튼은 원장 계정에만 보이고 서버도 마스터만 통과시킨다.
삭제 확인창에는 함께 지워질 제출 건수를 명시한다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_015KDw3RTJ9Hurhdt2NhkgTa"
```

---

### Task 11: 학교 설정 — 학년 드롭다운과 변경 확인창

**Files:**
- Modify: `index.html` — `rSchoolSettings(sc)` (3578행 근처)의 학년 입력칸, 저장 핸들러
- Modify: `api/admin/school/[id].js` — 저장 시 학년 정규화

**Interfaces:**
- Consumes: Task 1의 `normalizeGrade` (서버), `ST.schoolsSummary` 또는 `GET /api/admin/mock-rounds`의 `grades` (클라이언트)
- Produces: 없음 (기존 저장 경로에 검증만 얹는다)

**이 태스크가 왜 필요한가:** 진급은 반의 학년만 바꾸는 방식이라 이 입력칸이 모의고사의 유일한 연결 고리가 된다. 지금은 자유 텍스트라 `2학년`을 `2 학년`이나 `고2`로 치면 그 반 전원이 회차 대상에서 빠지고, **에러도 나지 않아** 선생님은 "왜 아무도 안 냈지" 하게 된다. **내년 3월 첫 진급 전에는 반드시 끝나야 한다.**

- [ ] **Step 1: 서버 쪽 정규화 (클라이언트만 믿지 않는다)**

`api/admin/school/[id].js`에서 `saveSchool`을 호출하기 전에 학년을 정규화한다. 파일 상단에 추가한다.
```javascript
import { normalizeGrade } from '../../_lib/mock.js';
```
PUT 처리(17행)의 두 줄을 바꾼다.

변경 전:
```javascript
      const body = req.body || {};
      const updated = await saveSchool(id, { ...body, id }, body.version);
```
변경 후:
```javascript
      const body = req.body || {};
      // 학년은 모의고사 회차와 반을 잇는 유일한 키라, 클라이언트 검증만 믿지 않고 서버에서도 정규화한다.
      // 정규화 결과가 빈 문자열이면 원래 값을 그대로 둔다(정규화가 값을 삼켜버리는 일 방지).
      const grade = normalizeGrade(body.grade) || body.grade;
      const updated = await saveSchool(id, { ...body, id, grade }, body.version);
```

- [ ] **Step 2: 학년 입력칸을 드롭다운으로**

`rSchoolSettings(sc)`에서 아래 한 줄을 바꾼다.

변경 전:
```javascript
      <div class="fgrp"><label class="flbl">학년</label><input class="finp" id="ss-grade" value="${sc.grade}"></div>
```
변경 후:
```javascript
      <div class="fgrp"><label class="flbl">학년</label>
        <select class="finp" id="ss-grade">
          ${knownGrades(sc.grade).map(g=>`<option value="${esc(g)}" ${g===sc.grade?'selected':''}>${esc(g)}</option>`).join('')}
          <option value="__new__">+ 새 학년 입력</option>
        </select>
        <input class="finp" id="ss-grade-new" placeholder="예: 3학년" style="display:none;margin-top:6px">
        <div style="font-size:11px;color:#9BA3AF;margin-top:5px">학년은 모의고사 회차와 반을 잇는 기준입니다. 표기가 흔들리면 그 반이 회차에서 빠집니다.</div>
      </div>
```

`function rSchoolSettings(sc){` **바로 위**에 헬퍼를 추가한다.
```javascript
// 현재 등록된 학년 목록 — 학교 요약 캐시에서 뽑는다. 지금 반의 학년은 목록에 없어도 반드시 포함한다.
function knownGrades(current){
  const src=(ST.schoolsSummary&&ST.schoolsSummary.length)?ST.schoolsSummary:DB.schools;
  const set=new Set((src||[]).map(s=>String(s.grade||'').trim()).filter(Boolean));
  if(current)set.add(String(current).trim());
  return [...set].sort();
}
```

- [ ] **Step 3: 드롭다운 토글과 변경 확인창**

`bAdmin()`의 `if(ST.atab==='school-settings'){` 블록(3894행 근처)에서 앞부분 다섯 줄을 바꾼다.

변경 전:
```javascript
    document.getElementById('btn-ss-save').onclick=()=>{
      const nm=document.getElementById('ss-name').value.trim();
      const gr=document.getElementById('ss-grade').value.trim();
      if(!nm){alert('학교명을 입력하세요');return;}
      sc.name=nm;sc.grade=gr;
```
변경 후:
```javascript
    const gSel=document.getElementById('ss-grade');
    const gNew=document.getElementById('ss-grade-new');
    if(gSel&&gNew){
      gSel.onchange=()=>{gNew.style.display=gSel.value==='__new__'?'block':'none';if(gSel.value==='__new__')gNew.focus();};
    }
    document.getElementById('btn-ss-save').onclick=()=>{
      const nm=document.getElementById('ss-name').value.trim();
      const gr=(gSel.value==='__new__'?(gNew.value||''):gSel.value).trim();
      if(!nm){alert('학교명을 입력하세요');return;}
      if(!gr){alert('학년을 고르거나 입력하세요');return;}
      // 학년이 바뀌면 그 반 학생 전원의 모의고사 대상 회차가 통째로 바뀐다 — 확인을 받는다
      if(gr!==sc.grade&&!confirm(`이 반을 [${gr}]으로 바꿉니다.\n\n학생 ${sc.students.length}명이 앞으로 ${gr} 모의고사 대상이 됩니다.\n지난 학년의 모의고사 기록은 그대로 남습니다.\n\n계속하시겠습니까?`))return;
      sc.name=nm;sc.grade=gr;
```

이 블록의 나머지(`sc.type=...`부터 `setTimeout(()=>render(),600);`까지)는 그대로 둔다.

- [ ] **Step 4: 문법 검사와 수동 확인**

Run:
```bash
node -e "const fs=require('fs');const h=fs.readFileSync('index.html','utf8');const m=[...h.matchAll(/<script>([\s\S]*?)<\/script>/g)];m.forEach(x=>{new Function(x[1]);});console.log('syntax OK, blocks:',m.length)"
npm test
```
Expected: `syntax OK, blocks: 2`, 테스트 전부 통과

수동 확인:
1. 학교 설정 → 학년이 드롭다운으로 바뀌었고, 현재 학년이 선택돼 있다
2. `+ 새 학년 입력`을 고르면 입력칸이 나타난다
3. 학년을 바꾸고 저장 → 학생 수가 들어간 확인창이 뜬다
4. 확인 → 저장되고, 그 반 학생의 앱에 새 학년 회차 입력창이 뜬다
5. 서버에 `고2`로 직접 PUT을 보내면 `2학년`으로 정규화되어 저장된다
6. 학년을 바꾼 뒤에도 학생 성적표의 지난 학년 회차 기록이 그대로 보인다

- [ ] **Step 5: 커밋**

```bash
git add index.html api/admin/school/[id].js
git commit -m "학교 설정의 학년을 드롭다운으로 바꾸고 변경 확인창 추가

진급이 반의 학년만 바꾸는 방식이라 이 입력칸이 모의고사 회차와 반을 잇는
유일한 고리가 된다. 자유 텍스트라 '2학년'을 '2 학년'으로 치면 그 반 전원이
회차에서 빠지는데 에러도 안 나서 알아채기 어렵다.

현존 학년 드롭다운 + 새 학년 직접 입력으로 바꾸고, 서버에서도 정규화한다.
학년을 바꿀 때는 학생 수와 '지난 기록은 남는다'는 안내를 담은 확인창을 띄운다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_015KDw3RTJ9Hurhdt2NhkgTa"
```

---

### Task 12: 변경 내역 갱신과 최종 검증

**Files:**
- Modify: `index.html` — CHANGELOG 배열 (571행 근처, 최신 항목이 배열 앞쪽)

**Interfaces:**
- Consumes: 없음
- Produces: 없음

- [ ] **Step 1: 기존 CHANGELOG 형식 확인**

Run: `grep -n "학생 성적표에 그 주차 반 최고점" index.html`
그 줄 주변 20줄을 읽어 날짜 키와 배열 형식을 그대로 따른다.

- [ ] **Step 2: 항목 추가**

가장 최신 항목 자리에 `2026-09-03` 블록을 추가한다. 문구는 학생·선생님이 읽는 것이므로 내부 용어(집계 캐시, 해시 등)를 쓰지 않는다.

```javascript
    '모의고사 점수를 학생이 직접 입력하고, 같은 학년 전체 기준 등수를 확인할 수 있는 기능 추가',
    '성적표에 모의고사 회차별 점수 추이와 학년 등수 추이 그래프 추가 (학년이 올라가도 이어서 표시)',
    '모의고사 입력 기간에는 로그인 시 입력 팝업이 떠서 점수만 빠르게 넣을 수 있게 함',
    '선생님 화면에 모의고사 탭 추가 — 우리 반 제출 현황·학년 등수, 반 평균과 학년 평균 비교',
    '학교 설정의 학년을 드롭다운으로 변경 (표기가 흔들리면 그 반이 모의고사 집계에서 빠지는 문제 예방)',
```

- [ ] **Step 3: 전체 검증**

Run:
```bash
npm test
node -e "const fs=require('fs');const h=fs.readFileSync('index.html','utf8');const m=[...h.matchAll(/<script>([\s\S]*?)<\/script>/g)];m.forEach(x=>{new Function(x[1]);});console.log('syntax OK, blocks:',m.length)"
ls api/*.js api/*/*.js api/*/*/*.js 2>/dev/null | grep -v '_lib' | wc -l
```
Expected:
- 테스트 전부 통과
- `syntax OK, blocks: 2`
- 마지막 줄이 **12** — Vercel Hobby 함수 한도를 넘지 않았는지 확인 (13 이상이면 배포가 실패한다)

- [ ] **Step 4: 커밋**

```bash
git add index.html
git commit -m "모의고사 기능 변경 내역 반영

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_015KDw3RTJ9Hurhdt2NhkgTa"
```

---

## 배포 후 확인

프로덕션 배포 뒤 아래 순서로 한 바퀴 돌린다.

1. 원장 계정 → 회차 관리에서 실제 회차 하나 개설 (마감일을 하루 뒤로)
2. 그 학년 학생 계정으로 로그인 → 팝업이 뜨는지, 제출되는지
3. 선생님 반 탭 → 그 학생이 제출됨으로, 나머지가 미제출로 보이는지
4. 응시자가 5명 미만인 동안 학생 화면에 등수가 **안** 나오는지
5. 마감일을 지난 시각으로 바꿔 저장 → 학생 화면에 등수·평균·등급분포가 나오는지
6. 테스트용 회차를 삭제하고, 확인창의 제출 건수가 맞는지
